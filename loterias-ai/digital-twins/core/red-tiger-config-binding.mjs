const eps=(a,b,tol=.005)=>Math.abs(Number(a)-Number(b))<=tol;
export function classifyRedTigerJackpotConfig({operatorPageRtpPct,withJackpotRtpVariantsPct=[],withoutJackpotRtpVariantsPct=[],tolerancePct=.005}={}){if(!Number.isFinite(Number(operatorPageRtpPct)))return {status:'UNKNOWN',reason:'OPERATOR_PAGE_RTP_REQUIRED'};const page=Number(operatorPageRtpPct);const withHits=withJackpotRtpVariantsPct.filter(x=>eps(page,x,tolerancePct));const withoutHits=withoutJackpotRtpVariantsPct.filter(x=>eps(page,x,tolerancePct));if(withHits.length&&withoutHits.length)return {status:'AMBIGUOUS',pageRtpPct:page,withHits,withoutHits,reason:'RTP_MATCHES_BOTH_CONFIG_FAMILIES'};if(withHits.length)return {status:'NATIVE_JACKPOT_RTP_MATCH',pageRtpPct:page,withHits,withoutHits:[],reason:'PAGE_RTP_MATCHES_EXACT_WITH_JACKPOT_VARIANT'};if(withoutHits.length)return {status:'NO_NATIVE_JACKPOT_RTP_MATCH',pageRtpPct:page,withHits:[],withoutHits,reason:'PAGE_RTP_MATCHES_EXACT_WITHOUT_JACKPOT_VARIANT'};return {status:'NO_EXACT_RTP_MATCH',pageRtpPct:page,withHits:[],withoutHits:[],reason:'CURRENT_PAGE_RTP_DOES_NOT_MATCH_SUPPLIED_EXACT_VARIANTS'};}
export function executionGuard(classification={}){return classification.status==='NATIVE_JACKPOT_RTP_MATCH'?{decision:'REVIEW_NATIVE_JACKPOT_RUNTIME',realMoneyAllowed:false,realStakeEUR:0}:{decision:'NO_PLAY',realMoneyAllowed:false,realStakeEUR:0,reason:classification.reason||'NATIVE_JACKPOT_NOT_BOUND'};}
