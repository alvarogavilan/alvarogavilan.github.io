import fs from 'node:fs';
const gatePath='loterias-ai/data/gates/real-money-status.json';
const g=fs.existsSync(gatePath)?JSON.parse(fs.readFileSync(gatePath,'utf8')):{games:[],summary:{}};
const required=['primitiva','bonoloto','gordo-primitiva','euromillones','eurodreams','loteria-nacional','quiniela','elige8','quinigol','lototurf','quintuple-plus'];
const map=new Map((g.games||[]).map(x=>[x.id,x]));const allowed=new Set(['ELIGIBLE','CANDIDATE_SHADOW','REJECTED','BLOCKED']);const unresolved=required.filter(id=>!allowed.has(map.get(id)?.status));
const ui=['loterias-ai/index.html','loterias-ai/hoy/index.html','loterias-ai/lab/index.html','loterias-ai/historico/index.html','loterias-ai/tesoreria/index.html'];
const uiPresent=ui.filter(fs.existsSync),out={generatedAt:new Date().toISOString(),scope:'Operational model-gate completion excluding deferred seasonal Navidad/El Niño deep work',mainGames:required.length,resolvedGames:required.length-unresolved.length,unresolvedGames:unresolved,modelGateCompletionPct:+(((required.length-unresolved.length)/required.length)*100).toFixed(2),ui:{required:ui.length,present:uiPresent.length,completionPct:+((uiPresent.length/ui.length)*100).toFixed(2)},moneyGate:g.summary||{},complete:unresolved.length===0&&uiPresent.length===ui.length,truth:'100% here means every non-seasonal main game has a deterministic current classification (eligible, shadow, rejected, or data-blocked) and the principal UI surfaces exist. It does not mean guaranteed profit, complete eternal history, or that blocked external data has magically become available.'};fs.writeFileSync('loterias-ai/data/gates/operational-completion.json',JSON.stringify(out,null,2)+'\n');console.log(JSON.stringify(out,null,2));