import fs from 'node:fs';
const dir='loterias-ai/data/archive/gordo-primitiva',max=54,pick=5,cost=1.5;
const strategies={balanced:{longFrequency:.20,shortMomentum:.15,gap:.15,stability:.15,pairAffinity:.10,entropy:.10,meanReversion:.15},frequency:{longFrequency:.45,shortMomentum:.25,gap:.05,stability:.10,pairAffinity:.05,entropy:.05,meanReversion:.05},gap:{longFrequency:.10,shortMomentum:.05,gap:.45,stability:.10,pairAffinity:.05,entropy:.10,meanReversion:.15},stability:{longFrequency:.15,shortMomentum:.10,gap:.10,stability:.40,pairAffinity:.10,entropy:.05,meanReversion:.10},cooccurrence:{longFrequency:.15,shortMomentum:.10,gap:.10,stability:.10,pairAffinity:.35,entropy:.10,meanReversion:.10}};
let records=[];for(const f of fs.readdirSync(dir).filter(x=>/^\d{4}\.json$/.test(x)).sort())records.push(...(JSON.parse(fs.readFileSync(`${dir}/${f}`,'utf8')).records||[]));records=[...new Map(records.map(r=>[r.drawId,r])).values()].sort((a,b)=>a.drawDate.localeCompare(b.drawDate));const eligible=records.filter(r=>r.economics?.validation?.officialSELAE&&Array.isArray(r.economics?.categories)&&r.economics.categories.length&&r.result?.main?.length===5&&Number.isInteger(Number(r.result?.key)));
const mean=a=>a.length?a.reduce((s,x)=>s+x,0)/a.length:0,sd=a=>{if(a.length<2)return 0;const m=mean(a);return Math.sqrt(a.reduce((s,x)=>s+(x-m)**2,0)/(a.length-1))},clamp=(x,a=0,b=1)=>Math.max(a,Math.min(b,x)),unit=z=>.5+.5*Math.tanh(z/2);
function z(c,n){const v=c.slice(1),m=mean(v),s=sd(v);return s?(c[n]-m)/s:0}function ranked(st,w,i){const gaps=Array(max+1).fill(0);let gm=1;for(let n=1;n<=max;n++){gaps[n]=st.last[n]<0?i:i-1-st.last[n];gm=Math.max(gm,gaps[n])}const total=st.all.slice(1).reduce((a,b)=>a+b,0)||1,o=[];for(let n=1;n<=max;n++){const za=z(st.all,n),z20=z(st.c20,n),z50=z(st.c50,n),z100=z(st.c100,n),lf=unit(za),sm=clamp(.6*unit(z20)+.4*unit(z50)),gap=clamp(gaps[n]/gm),stab=clamp(1-(Math.abs(z20-z50)+Math.abs(z50-z100)+Math.abs(z100-za))/8);let pa=0;if(st.all[n])for(let x=1;x<=max;x++)if(x!==n)pa=Math.max(pa,st.pairs[n][x]/st.all[n]);const p=st.all[n]/total,ent=clamp((p>0?-p*Math.log(p):0)*8),mr=clamp(1-Math.abs(unit(za)-.5)*2),s={longFrequency:lf,shortMomentum:sm,gap,stability:stab,pairAffinity:clamp(pa),entropy:ent,meanReversion:mr};o.push({number:n,score:Object.entries(w).reduce((a,[k,v])=>a+s[k]*v,0)})}return o.sort((a,b)=>b.score-a.score||a.number-b.number)}
function portfolio(r,n,keyCounts){const use=new Map(),keys=Array.from({length:10},(_,k)=>k).sort((a,b)=>keyCounts[b]-keyCounts[a]||a-b),o=[];for(let t=0;t<n;t++){const q=r.map(x=>({x,a:x.score-(use.get(x.number)||0)*.10})).sort((a,b)=>b.a-a.a||a.x.number-b.x.number),main=q.slice(0,pick).map(y=>y.x.number).sort((a,b)=>a-b);main.forEach(k=>use.set(k,(use.get(k)||0)+1));o.push({main,key:keys[t%10]})}return o}
function add(st,r,i){const ns=r.result.main.map(Number);st.q20.push(ns);st.q50.push(ns);st.q100.push(ns);for(const n of ns){st.all[n]++;st.c20[n]++;st.c50[n]++;st.c100[n]++;st.last[n]=i;for(const x of ns)if(x!==n)st.pairs[n][x]++;}for(const[q,c,l]of[[st.q20,st.c20,20],[st.q50,st.c50,50],[st.q100,st.c100,100]])if(q.length>l)for(const n of q.shift())c[n]--;const k=Number(r.result.key);if(k>=0&&k<=9)st.keys[k]++;}
function prize(rec,t){const win=rec.result.main.map(Number),h=t.main.filter(n=>win.includes(n)).length,key=Number(t.key)===Number(rec.result.key),cat=h===5?(key?1:2):h===4?(key?3:4):h===3?(key?5:6):h===2?(key?7:8):key?9:null;if(!cat)return 0;return Number(rec.economics.categories.find(x=>Number(x.category)===cat)?.prize||0)}
const cut=Math.floor(eligible.length*.70),discIds=new Set(eligible.slice(0,cut).map(r=>r.drawId)),holdIds=new Set(eligible.slice(cut).map(r=>r.drawId)),b={};for(const s of Object.keys(strategies))for(let n=1;n<=5;n++)b[`${s}|${n}`]={strategy:s,tickets:n,discovery:{draws:0,stake:0,return:0},holdout:{draws:0,stake:0,return:0}};
const st={all:Array(max+1).fill(0),c20:Array(max+1).fill(0),c50:Array(max+1).fill(0),c100:Array(max+1).fill(0),last:Array(max+1).fill(-1),pairs:Array.from({length:max+1},()=>Array(max+1).fill(0)),q20:[],q50:[],q100:[],keys:Array(10).fill(0)};for(let i=0;i<records.length;i++){const r=records[i];if(i>=200&&r.economics?.validation?.officialSELAE){for(const[s,w]of Object.entries(strategies)){const rankedMain=ranked(st,w,i);for(let n=1;n<=5;n++){const side=discIds.has(r.drawId)?'discovery':holdIds.has(r.drawId)?'holdout':null;if(!side)continue;const tickets=portfolio(rankedMain,n,st.keys),ret=tickets.reduce((a,t)=>a+prize(r,t),0),x=b[`${s}|${n}`][side];x.draws++;x.stake+=n*cost;x.return+=ret}}}add(st,r,i)}
const sum=x=>({...x,pl:x.return-x.stake,roi:x.stake?(x.return-x.stake)/x.stake:null}),ranking=Object.values(b).map(x=>({strategy:x.strategy,tickets:x.tickets,discovery:sum(x.discovery),holdout:sum(x.holdout)})).sort((a,b)=>(b.discovery.roi??-99)-(a.discovery.roi??-99)),selected=ranking[0];function rng(seed){let x=seed>>>0;return()=>((x=(1664525*x+1013904223)>>>0)/4294967296)}function sampleMain(r){const a=Array.from({length:max},(_,i)=>i+1),o=[];while(o.length<pick)o.push(a.splice(Math.floor(r()*a.length),1)[0]);return o.sort((a,b)=>a-b)}const hold=eligible.slice(cut),base=[];for(let seed=0;seed<100;seed++){const rr=rng(23000+seed);let stake=0,ret=0;for(const rec of hold){for(let j=0;j<selected.tickets;j++){ret+=prize(rec,{main:sampleMain(rr),key:Math.floor(rr()*10)});stake+=cost}}base.push(stake?(ret-stake)/stake:null)}const valid=base.filter(Number.isFinite),avg=mean(valid),mx=valid.length?Math.max(...valid):null,pass=selected.holdout.draws>=100&&selected.holdout.roi>0&&mx!==null&&selected.holdout.roi>mx;const out={generatedAt:new Date().toISOString(),gameId:'gordo-primitiva',source:'SELAE official economics only',officialEconomicDraws:eligible.length,methodology:'70-30 discovery-locked holdout; main-number strategy + diversified historical Clave ranking',selected:{strategy:selected.strategy,tickets:selected.tickets,discoveryROI:selected.discovery.roi},holdout:selected.holdout,randomBaseline:{runs:valid.length,averageROI:avg,maxROI:mx},minimumHoldoutDraws:100,realMoneyPass:pass,ranking,truth:'Both main numbers and Clave are modeled using only information available before each draw. Holdout is not used for model selection.'};fs.mkdirSync('loterias-ai/data/backtests',{recursive:true});fs.writeFileSync('loterias-ai/data/backtests/gordo-primitiva-official-financial-tournament.json',JSON.stringify(out,null,2)+'\n');console.log(JSON.stringify({official:eligible.length,selected:out.selected,holdout:out.holdout,baseline:out.randomBaseline,pass},null,2));