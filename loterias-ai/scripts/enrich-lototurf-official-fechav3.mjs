import fs from 'node:fs';
const dir='loterias-ai/data/archive/lototurf',meta='loterias-ai/data/archive/_meta';fs.mkdirSync(meta,{recursive:true});const limit=Math.max(1,Number(process.env.BATCH_LIMIT||60));
const files=fs.existsSync(dir)?fs.readdirSync(dir).filter(f=>/^\d{4}\.json$/.test(f)).sort():[];const docs=new Map(),targets=[];
for(const f of files){const p=`${dir}/${f}`,doc=JSON.parse(fs.readFileSync(p,'utf8'));docs.set(f,{p,doc});for(const r of doc.records||[])if(r.drawDate&&!r.economics?.validation?.officialSELAE)targets.push(r)}targets.sort((a,b)=>b.drawDate.localeCompare(a.drawDate));
const num=x=>{const n=Number(String(x??'').replace(/\./g,'').replace(',','.'));return Number.isFinite(n)?n:null};const sleep=ms=>new Promise(r=>setTimeout(r,ms));
async function get(date){const d=date.replaceAll('-',''),url=`https://www.loteriasyapuestas.es/servicios/fechav3?game_id=LOTU&fecha_sorteo=${d}`;let err;for(let a=1;a<=3;a++){try{const r=await fetch(url,{redirect:'follow',signal:AbortSignal.timeout(18000),headers:{'user-agent':'LoteriasAI/1.0 research archive','accept':'*/*'}});if(r.ok){const j=await r.json();return{url,row:Array.isArray(j)?j[0]:null}}err=new Error(`HTTP ${r.status}`)}catch(e){err=e}await sleep(a*700)}throw err}
function parseCombo(s){const m=String(s||'').match(/^(.*?)\s+C\((\d+)\)\s+R\((\d+)\)/i);if(!m)return null;const main=(m[1].match(/\d+/g)||[]).map(Number);if(main.length!==6)return null;return{main:[...main].sort((a,b)=>a-b),horse:Number(m[2]),reintegro:Number(m[3])}}
let updated=0,mismatch=0,empty=0;const failures=[],quarantine=[];for(const [i,r] of targets.slice(0,limit).entries()){try{const {url,row}=await get(r.drawDate);if(!row){empty++;continue}const c=parseCombo(row.combinacion);if(!c){failures.push({date:r.drawDate,reason:'combo-parse'});continue}const current=(r.result?.main||[]).map(Number).sort((a,b)=>a-b);if(current.length&&current.join(',')!==c.main.join(',')){mismatch++;const q={date:r.drawDate,reason:'main-mismatch',internal:current,official:c.main,officialUrl:url};failures.push(q);quarantine.push(q);continue}if(r.result?.horse!=null&&Number(r.result.horse)!==c.horse){mismatch++;const q={date:r.drawDate,reason:'horse-mismatch',internal:r.result.horse,official:c.horse,officialUrl:url};failures.push(q);quarantine.push(q);continue}r.result=c;r.drawNumber=row.id_sorteo;r.economics={currency:'EUR',ticketCost:1,stakes:num(row.apuestas),revenue:num(row.recaudacion),jackpot:num(row.premio_bote),prizePool:num(row.premios),rolloverFund:num(row.fondo_bote),categories:(row.escrutinio||[]).map(x=>({category:Number(x.categoria),label:String(x.tipo||'').trim(),winners:num(x.ganadores),prize:num(x.premio)})),source:{provider:'SELAE',url,capturedAt:new Date().toISOString()},validation:{status:'OFFICIAL_PARSED',officialSELAE:true}};r.source={provider:'SELAE',tier:'official',url,validation:'official-fechav3',archivePreviousSource:r.source};updated++;}catch(e){failures.push({date:r.drawDate,reason:String(e)})}if(i%15===14)await sleep(350)}
for(const {p,doc} of docs.values())fs.writeFileSync(p,JSON.stringify(doc,null,2)+'\n');let total=0,official=0;for(const {doc} of docs.values())for(const r of doc.records||[]){total++;if(r.economics?.validation?.officialSELAE)official++}const out={generatedAt:new Date().toISOString(),gameId:'lototurf',batchLimit:limit,attempted:Math.min(limit,targets.length),updated,empty,mismatch,failures,totalRecords:total,officialEconomicsRecords:official,remaining:Math.max(0,total-official),qualityPass:mismatch===0,quarantinedMismatchCount:quarantine.length,next:quarantine.length?'review-quarantined-mismatches-while-continuing-nonconflicting-official-enrichment':'continue-official-enrichment'};fs.writeFileSync(`${meta}/lototurf-official-enrichment.json`,JSON.stringify(out,null,2)+'\n');fs.writeFileSync(`${meta}/lototurf-official-mismatch-quarantine.json`,JSON.stringify({generatedAt:out.generatedAt,gameId:'lototurf',count:quarantine.length,items:quarantine,policy:'Never overwrite an internal archived result when SELAE disagrees. Quarantine for manual/source reconciliation; continue enriching non-conflicting records.'},null,2)+'\n');console.log(JSON.stringify({...out,failures:failures.length},null,2));
