import fs from 'node:fs';
const TARGETS=new Map([['2024-01-11',22],['2026-07-25',38]]),max=49,minHistory=250,keys=['longFrequency','shortMomentum','gap','stability','pairAffinity','entropy','meanReversion'],w={longFrequency:.2436021948629059,shortMomentum:.23052522678219245,gap:.1375835356389871,stability:.25253420687920874,pairAffinity:.07113006157102063,entropy:.07939597515272907,meanReversion:.24348168524738867};
const mean=a=>a.length?a.reduce((s,x)=>s+x,0)/a.length:0,sd=a=>{if(a.length<2)return 0;const m=mean(a);return Math.sqrt(a.reduce((s,x)=>s+(x-m)**2,0)/(a.length-1))},clamp=(x,a=0,b=1)=>Math.max(a,Math.min(b,x)),unit=z=>.5+.5*Math.tanh(z/2),z=(c,n)=>{const v=c.slice(1),m=mean(v),s=sd(v);return s?(c[n]-m)/s:0};
function features(st,i){const gaps=Array(max+1).fill(0);let gm=1;for(let n=1;n<=max;n++){gaps[n]=st.last[n]<0?i:i-1-st.last[n];gm=Math.max(gm,gaps[n])}const total=st.all.slice(1).reduce((a,b)=>a+b,0)||1,o=[];for(let n=1;n<=max;n++){const za=z(st.all,n),z20=z(st.c20,n),z50=z(st.c50,n),z100=z(st.c100,n),lf=unit(za),sm=clamp(.6*unit(z20)+.4*unit(z50)),gap=clamp(gaps[n]/gm),stab=clamp(1-(Math.abs(z20-z50)+Math.abs(z50-z100)+Math.abs(z100-za))/8);let pa=0;if(st.all[n])for(let x=1;x<=max;x++)if(x!==n)pa=Math.max(pa,st.pairs[n][x]/st.all[n]);const p=st.all[n]/total,ent=clamp((p>0?-p*Math.log(p):0)*8),mr=clamp(1-Math.abs(unit(za)-.5)*2);o.push({number:n,f:{longFrequency:lf,shortMomentum:sm,gap,stability:stab,pairAffinity:clamp(pa),entropy:ent,meanReversion:mr}})}return o}function rank(fr){const den=Object.values(w).reduce((a,b)=>a+b,0);return fr.map(x=>({number:x.number,score:keys.reduce((s,k)=>s+x.f[k]*w[k],0)/den,f:x.f})).sort((a,b)=>b.score-a.score||a.number-b.number)}function add(st,ns,i){st.q20.push(ns);st.q50.push(ns);st.q100.push(ns);for(const n of ns){st.all[n]++;st.c20[n]++;st.c50[n]++;st.c100[n]++;st.last[n]=i;for(const x of ns)if(x!==n)st.pairs[n][x]++}for(const[q,c,l]of[[st.q20,st.c20,20],[st.q50,st.c50,50],[st.q100,st.c100,100]])if(q.length>l)for(const n of q.shift())c[n]--}
let rows=[];for(const f of fs.readdirSync('loterias-ai/data/archive/primitiva').filter(f=>/^\d{4}\.json$/.test(f)).sort())rows.push(...(JSON.parse(fs.readFileSync(`loterias-ai/data/archive/primitiva/${f}`,'utf8')).records||[]));rows=[...new Map(rows.map(r=>[r.drawId,r])).values()].filter(r=>r.result?.main?.length===6).sort((a,b)=>a.drawDate.localeCompare(b.drawDate));const st={all:Array(50).fill(0),c20:Array(50).fill(0),c50:Array(50).fill(0),c100:Array(50).fill(0),last:Array(50).fill(-1),pairs:Array.from({length:50},()=>Array(50).fill(0)),q20:[],q50:[],q100:[]},events=[];
for(let i=0;i<rows.length;i++){const r=rows[i],truth=r.result.main.map(Number);if(i>=minHistory&&TARGETS.has(r.drawDate)){const ranked=rank(features(st,i)),pool=ranked.slice(0,8).map(x=>x.number),T=new Set(truth),missing=TARGETS.get(r.drawDate),missingRow=ranked.find(x=>x.number===missing),falsePositives=pool.filter(n=>!T.has(n));events.push({date:r.drawDate,truth,pool,poolHits:pool.filter(n=>T.has(n)).length,missingNumber:missing,missingRank:ranked.findIndex(x=>x.number===missing)+1,missingScore:missingRow?.score,missingFeatures:missingRow?.f,falsePositives:falsePositives.map(n=>{const x=ranked.find(z=>z.number===n);return{number:n,rank:ranked.findIndex(z=>z.number===n)+1,score:x?.score,features:x?.f}}),top20:ranked.slice(0,20).map((x,j)=>({rank:j+1,number:x.number,score:x.score,isWinner:T.has(x.number)}))})}add(st,truth,i)}
const out={generatedAt:new Date().toISOString(),version:'v185',gameId:'primitiva',family:'V126_C46_SIXTH_ANATOMY',methodology:'Exact pre-draw reconstruction of the two repeated c46 5/6 events. No parameter fitting: record missing winner rank/features and false positives inside the frozen top-8.',events,realMoneyPass:false,realStakeEUR:0};fs.writeFileSync('loterias-ai/data/research/meta-pleno-v185-primitiva-sixth-anatomy.json',JSON.stringify(out,null,2)+'\n');console.log(JSON.stringify(events.map(e=>({date:e.date,missing:e.missingNumber,missingRank:e.missingRank,falsePositives:e.falsePositives.map(x=>x.number)}))));