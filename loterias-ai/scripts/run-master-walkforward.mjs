import fs from 'node:fs';import vm from 'node:vm';
function loadEngine(){const c={globalThis:{},window:undefined,console,Math,Number,String,Array,Set,Object,Map,Date,JSON};vm.createContext(c);vm.runInContext(fs.readFileSync('loterias-ai/js/mega-numeric-engine.js','utf8'),c);const m=c.globalThis.LotteryMegaNumericEngine;const tctx={globalThis:{LotteryMegaNumericEngine:m},window:undefined,console,Math,Number,String,Array,Set,Object,Map,Date,JSON};vm.createContext(tctx);vm.runInContext(fs.readFileSync('loterias-ai/js/walk-forward-tournament.js','utf8'),tctx);return tctx.globalThis.LotteryWalkForwardTournament;}
const T=loadEngine();const games={bonoloto:{id:'bonoloto',main:{max:49,pick:6}},primitiva:{id:'primitiva',main:{max:49,pick:6}},euromillones:{id:'euromillones',main:{max:50,pick:5}},eurodreams:{id:'eurodreams',main:{max:40,pick:6}},'gordo-primitiva':{id:'gordo-primitiva',main:{max:54,pick:5}}};
const out={generatedAt:new Date().toISOString(),mode:'RESEARCH_ONLY',methodology:'strict-chronological-multi-year-walk-forward',games:{}};
for(const [id,game] of Object.entries(games)){const dir=`loterias-ai/data/archive/${id}`;if(!fs.existsSync(dir)){out.games[id]={status:'archive_missing'};continue;}const files=fs.readdirSync(dir).filter(f=>/^\d{4}\.json$/.test(f)).sort();let raw=[];for(const f of files){const x=JSON.parse(fs.readFileSync(`${dir}/${f}`,'utf8'));raw.push(...(x.records||[]));}raw=[...new Map(raw.map(r=>[r.drawId,r])).values()].sort((a,b)=>a.drawDate.localeCompare(b.drawDate));const draws=raw.map(r=>({drawId:r.drawId,drawDate:r.drawDate,numbers:(r.result?.main||[]).map(Number)})).filter(d=>d.numbers.length===game.main.pick&&d.numbers.every(Number.isFinite));const min=Math.max(60,Math.min(250,Math.floor(draws.length*.35)));if(draws.length<=min){out.games[id]={status:'insufficient_sample',draws:draws.length,years:files,minHistory:min};continue;}const r=T.run({draws,game,minHistory:min,strategies:T.defaultStrategies()});out.games[id]={status:r.status,years:files.map(x=>x.slice(0,4)),draws:draws.length,minHistory:min,winner:r.winner&&{id:r.winner.id,config:r.winner.config,drawsEvaluated:r.winner.drawsEvaluated,averageHits:r.winner.averageHits,bestTicketAverageHits:r.winner.bestTicketAverageHits,hitRateAtLeast2:r.winner.hitRateAtLeast2,hitRateAtLeast3:r.winner.hitRateAtLeast3,confidence95:r.winner.confidence95},strategies:r.strategies.map(s=>({id:s.id,drawsEvaluated:s.drawsEvaluated,averageHits:s.averageHits,bestTicketAverageHits:s.bestTicketAverageHits,hitRateAtLeast2:s.hitRateAtLeast2,hitRateAtLeast3:s.hitRateAtLeast3,confidence95:s.confidence95}))};}
fs.mkdirSync('loterias-ai/data/backtests',{recursive:true});fs.writeFileSync('loterias-ai/data/backtests/master-walkforward.json',JSON.stringify(out,null,2)+'\n');console.log(JSON.stringify(out.games,null,2));