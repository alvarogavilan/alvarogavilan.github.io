#!/usr/bin/env node
import fs from 'node:fs';
import crypto from 'node:crypto';

const archive='loterias-ai/data/archive/quinigol',freezePath='loterias-ai/data/policies/quinigol-replication-v4-freeze.json',ledgerPath='loterias-ai/data/shadow/quinigol-replication-v4-ledger.json',outPath='loterias-ai/data/shadow/quinigol-replication-v4-final-30.json';
const freeze=JSON.parse(fs.readFileSync(freezePath,'utf8')),ledger=fs.existsSync(ledgerPath)?JSON.parse(fs.readFileSync(ledgerPath,'utf8')):null;
const boundary=Number(freeze.fixedBoundary?.jornadas),reps=Number(freeze.fixedBoundary?.matchedRandomReplicates),alpha=Number(freeze.fixedBoundary?.alpha),seed0=Number(freeze.fixedBoundary?.matchedRandomSeed)>>>0,minProfitable=Number(freeze.fixedBoundary?.minimumProfitableJornadas);
if(freeze.version!=='quinigol-independent-replication-v4-freeze-v1'||boundary!==30||reps!==10000||alpha!==0.01||minProfitable!==5||freeze.guards?.realMoneyAllowed!==false)throw new Error('Quinigol V4 finalization freeze drift');
if(!ledger||ledger.activation?.v3Fixed30Complete!==true){console.log(JSON.stringify({status:'WAITING_FOR_V3_FIXED_30'}));process.exit(0)}
const sha=x=>crypto.createHash('sha256').update(JSON.stringify(x)).digest('hex');
const predictionHash=e=>sha({anchorLastOfficialDrawId:e.anchorLastOfficialDrawId,anchorLastOfficialDate:e.anchorLastOfficialDate,tickets:e.tickets,model:e.model,v3BoundaryLastTarget:e.v3BoundaryLastTarget});
const observed=ledger.entries.filter(e=>e.status==='OBSERVED_OFFICIAL_BLINDED_V4');if(observed.length<boundary){console.log(JSON.stringify({status:'WAITING_FOR_V4_FIXED_30',observed:observed.length,remaining:boundary-observed.length}));process.exit(0)}if(observed.length!==boundary)throw new Error('V4 post-30 observations forbidden');if(ledger.entries.some(e=>e.status==='PREDICTED_SHADOW_V4'))throw new Error('V4 pending prediction remains at fixed boundary');
let rows=[];for(const f of fs.readdirSync(archive).filter(x=>/^\d{4}\.json$/.test(x)).sort())rows.push(...(JSON.parse(fs.readFileSync(`${archive}/${f}`,'utf8')).records||[]));rows=rows.filter(r=>r.drawDate&&Array.isArray(r.result?.codes)&&r.result.codes.length===6&&r.economics?.validation?.officialSELAE).sort((a,b)=>a.drawDate.localeCompare(b.drawDate));
const byId=new Map(rows.map(r=>[r.drawId,r])),byDate=new Map(rows.map(r=>[r.drawDate,r]));const buckets=['0','1','2','M'],codes=[];for(const a of buckets)for(const b of buckets)codes.push(`${a}-${b}`);
const payout=(rec,hits)=>{for(const c of rec.economics?.categories||[]){const l=String(c.label||'').toLowerCase();if(new RegExp(`(^|\\D)${hits}\\s*aciert`).test(l))return Number(c.prize||0)}return 0};const evalTicket=(rec,t)=>{let h=0;for(let i=0;i<6;i++)if(t[i]===String(rec.result.codes[i]).toUpperCase())h++;return{hits:h,ret:payout(rec,h)}};
const v3Boundary=String(ledger.activation.v3BoundaryLastTarget),targets=[];for(const e of observed){const rec=(e.drawId&&byId.get(e.drawId))||byDate.get(e.drawDate);if(!rec)throw new Error(`missing V4 official target ${e.drawId||e.drawDate}`);if(!(rec.drawDate>v3Boundary&&rec.drawDate>e.anchorLastOfficialDate))throw new Error(`V4 target not strictly future ${e.entryId}`);if(e.model?.window!==320||e.model?.ticketsPerDraw!==2||e.tickets?.length!==2||Number(e.realStakeEUR||0)!==0)throw new Error(`V4 custody drift ${e.entryId}`);if(predictionHash(e)!==e.predictionSha256)throw new Error(`V4 prediction hash failed ${e.entryId}`);const evidence={drawId:rec.drawId||null,drawDate:rec.drawDate,resultCodes:rec.result.codes,economics:rec.economics};if(sha(evidence)!==e.officialEvidenceSha256)throw new Error(`V4 official evidence hash failed ${e.entryId}`);targets.push({e,rec})}
let stake=0,ret=0,profitable=0,bestHits=0,hitHistogram=[0,0,0,0,0,0,0];for(const {e,rec} of targets){let ds=0,dr=0;for(const t of e.tickets){const x=evalTicket(rec,t);ds+=Number(rec.economics.ticketCost||1);dr+=x.ret;bestHits=Math.max(bestHits,x.hits);hitHistogram[x.hits]++}stake+=ds;ret+=dr;if(dr>ds)profitable++}const roi=stake?(ret-stake)/stake:null;
function rng(seed){let x=seed>>>0;return()=>((x=(1664525*x+1013904223)>>>0)/4294967296)}function randomTicket(rr){return Array.from({length:6},()=>codes[Math.floor(rr()*codes.length)])}const nullRois=[];for(let rep=0;rep<reps;rep++){const rr=rng((seed0+Math.imul(rep,2654435761))>>>0);let bs=0,br=0;for(const {rec} of targets)for(let k=0;k<2;k++){const x=evalTicket(rec,randomTicket(rr));bs+=Number(rec.economics.ticketCost||1);br+=x.ret}nullRois.push(bs?(br-bs)/bs:null)}
const valid=nullRois.filter(Number.isFinite),mean=a=>a.reduce((s,x)=>s+x,0)/a.length,sorted=[...valid].sort((a,b)=>a-b),quant=p=>sorted[Math.min(sorted.length-1,Math.max(0,Math.floor(p*(sorted.length-1))))],nullMean=mean(valid),ge=valid.filter(x=>x>=roi).length,pUpper=(1+ge)/(valid.length+1),pass=roi>0&&profitable>=minProfitable&&roi>nullMean&&pUpper<alpha;
let v3=null;try{v3=JSON.parse(fs.readFileSync('loterias-ai/data/shadow/quinigol-prospective-final-30.json','utf8'))}catch{}const v3Pass=v3?.gates?.economicPromotionCandidate===true;
const out={version:'quinigol-independent-replication-v4-final-30-v1',generatedAt:new Date().toISOString(),freezeVersion:freeze.version,fixedBoundary:{jornadas:30,v3BoundaryLastTarget:v3Boundary,firstTarget:targets[0].rec.drawDate,lastTarget:targets.at(-1).rec.drawDate,post30CanRescue:false},observed:{stakeEUR:stake,returnEUR:ret,plEUR:ret-stake,roi,profitableJornadas:profitable,bestHits,hitHistogramAcross60Tickets:hitHistogram},matchedRandom:{replicates:valid.length,meanROI:nullMean,p50ROI:quant(.5),p95ROI:quant(.95),p99ROI:quant(.99),maxROI:sorted.at(-1),randomAtLeastObserved:ge,oneSidedPUpper:pUpper},gates:{positiveROI:roi>0,minimumProfitableJornadas:profitable>=minProfitable,beatsMatchedRandomMean:roi>nullMean,pBelowAlpha:pUpper<alpha,v4EconomicPass:pass,v3EconomicPass:v3Pass,independentConfirmation:v3Pass&&pass,realMoneyPass:false},interpretation:v3Pass&&pass?'TWO_SEQUENTIAL_PREREGISTERED_QUINIGOL_PROSPECTIVES_PASSED_BUT_REAL_MONEY_STILL_REQUIRES_EXPLICIT_REVIEW':pass?'V4_PASSED_BUT_V3_DID_NOT_CONFIRM':v3Pass?'V3_PASSED_BUT_V4_DID_NOT_REPLICATE':'NO_TWO_SAMPLE_CONFIRMATION',guards:{preRegisteredBeforeAnyV3Outcome:true,activationIndependentOfV3Outcome:true,all30V4PredictionsSealedBeforeResult:true,predictionHashesVerified:true,officialEvidenceHashesVerified:true,noRetuning:true,noOptionalStopping:true,post30RescueAllowed:false,automaticBettingAllowed:false,realMoneyAllowed:false,realStakeEUR:0}};fs.writeFileSync(outPath,JSON.stringify(out,null,2)+'\n');console.log(JSON.stringify(out,null,2));
