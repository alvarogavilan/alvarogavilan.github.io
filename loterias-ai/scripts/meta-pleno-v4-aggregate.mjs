import fs from 'node:fs';
const games=['primitiva','bonoloto','euromillones','eurodreams','gordo-primitiva'];const cfgs={primitiva:{dir:'primitiva',max:49,k:6},bonoloto:{dir:'bonoloto',max:49,k:6},euromillones:{dir:'euromillones',max:50,k:5},eurodreams:{dir:'eurodreams',max:40,k:6},'gordo-primitiva':{dir:'gordo-primitiva',max:54,k:5}};
const mod=(x,m)=>((x-1)%m+m)%m+1;function hit(p,t){let h=0;for(const x of p)if(t.includes(x))h++;return h}
const out={generatedAt:new Date().toISOString(),engine:'MetaPleno-v4-symbolic',policy:'Champion selected from discovery+validation only; final 20% opened once. Main-combination pleno only. Any positive signal remains SHADOW_ONLY.',games:{},realMoneyPass:false};
for(const game of games){const c=cfgs[game],sh=[];for(let i=0;i<4;i++){const p=`loterias-ai/data/research/meta-pleno-v4-shards/${game}-${i}.json`;if(fs.existsSync(p))sh.push(JSON.parse(fs.readFileSync(p,'utf8')))}if(sh.length!==4){out.games[game]={status:'INCOMPLETE_SHARDS',shards:sh.length};continue}const champion=sh.flatMap(x=>x.top||[]).sort((a,b)=>b.rank-a.rank)[0];let rows=[];for(const f of fs.readdirSync(`loterias-ai/data/archive/${c.dir}`).filter(x=>/^\d{4}\.json$/.test(x)).sort())rows.push(...(JSON.parse(fs.readFileSync(`loterias-ai/data/archive/${c.dir}/${f}`,'utf8')).records||[]));rows=rows.filter(r=>r.drawDate&&Array.isArray(r.result?.main)&&r.result.main.length===c.k).sort((a,b)=>a.drawDate.localeCompare(b.drawDate));const start=Math.floor(rows.length*.8),s=champion.spec;
 function predict(i){const a=rows[i-s.lag1]?.result?.main||[],b=rows[i-s.lag2]?.result?.main||[],dt=new Date(`${rows[i].drawDate}T12:00:00Z`),day=dt.getUTCDate(),mon=dt.getUTCMonth()+1,dow=dt.getUTCDay(),o=[];for(let q=0;q<c.k;q++){const x=a[(s.p1+q+s.rot)%c.k]||q+1,y=b[(s.p2+(s.mirror?c.k-1-q:q))%c.k]||q+1;let z;if(s.mode===0)z=s.a*x+s.b*y+s.d*day+s.e*mon+s.f+s.step*q;else if(s.mode===1)z=s.a*(x-y)+s.b*(x+y)+s.d*dow+s.e*q+s.f;else if(s.mode===2)z=s.a*x+s.b*y+s.d*(x%10)+s.e*(y%10)+s.f+s.step*(day+q);else z=s.a*(x+q)+s.b*(y-q)+s.d*(day+mon)+s.e*dow+s.f;let v=mod(z,c.max),g=0;while(o.includes(v)&&g++<c.max)v=mod(v+s.step,c.max);o.push(v)}return o.sort((x,y)=>x-y)}
 let exact=0,near=0,sum=0;const samples=[];for(let i=start;i<rows.length;i++){const h=hit(predict(i),rows[i].result.main);sum+=h;if(h===c.k){exact++;if(samples.length<20)samples.push({date:rows[i].drawDate,prediction:predict(i),truth:rows[i].result.main})}if(h>=c.k-1)near++}const draws=rows.length-start,space=(()=>{let r=1;for(let i=1;i<=c.k;i++)r=r*(c.max-c.k+i)/i;return r})(),pAny=1-Math.pow(1-1/space,draws),signal=(champion.validation?.exact||0)>=1&&exact>=2;out.games[game]={status:signal?'SYMBOLIC_REPEAT_RESEARCH_SIGNAL':'NO_SYMBOLIC_PLENO_EDGE',candidatesEvaluated:sh.reduce((a,x)=>a+(x.candidatesActuallyTested||0),0),champion,holdout:{draws,exactMain:exact,near,meanHits:sum/Math.max(1,draws),fullHitSamples:samples},null:{combinationSpace:space,expectedFullHits:draws/space,probabilityAtLeastOneRandom:pAny},repeatedPlenoSignal:signal,realMoneyPass:false}}
fs.mkdirSync('loterias-ai/data/research',{recursive:true});fs.writeFileSync('loterias-ai/data/research/meta-pleno-v4-symbolic.json',JSON.stringify(out,null,2)+'\n');console.log(JSON.stringify(out,null,2));