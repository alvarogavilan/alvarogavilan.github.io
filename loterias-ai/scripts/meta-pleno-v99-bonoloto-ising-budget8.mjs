import fs from 'node:fs';
const DIR='loterias-ai/data/archive/bonoloto',OUT='loterias-ai/data/research/meta-pleno-v99-bonoloto-ising-budget8.json';let rows=[];for(const f of fs.readdirSync(DIR).filter(x=>/^\d{4}\.json$/.test(x)).sort())rows.push(...(JSON.parse(fs.readFileSync(`${DIR}/${f}`,'utf8')).records||[]));rows=rows.filter(r=>r.drawDate&&Array.isArray(r.result?.main)&&r.result.main.length===6&&new Set(r.result.main).size===6).sort((a,b)=>a.drawDate.localeCompare(b.drawDate));const sets=rows.map(r=>new Set(r.result.main));
function pool(i,c){const lo=Math.max(0,i-c.window),freq=new Float64Array(50),pair=Array.from({length:50},()=>new Float64Array(50));for(let j=lo;j<i;j++){const a=[...sets[j]];for(const n of a)freq[n]++;for(let x=0;x<a.length;x++)for(let y=x+1;y<a.length;y++){pair[a[x]][a[y]]++;pair[a[y]][a[x]]++;}}const base=Array.from({length:49},(_,z)=>z+1).map(n=>({n,s:Math.log((freq[n]+c.alpha)/(i-lo+49*c.alpha))})).sort((a,b)=>b.s-a.s||a.n-b.n);const chosen=[];for(const cand of base){let s=cand.s;for(const q of chosen){const pij=(pair[cand.n][q]+c.beta)/(Math.max(1,freq[q])+49*c.beta);s+=c.lambda*Math.log(pij+1e-12);}chosen.push({n:cand.n,s});chosen.sort((a,b)=>b.s-a.s||a.n-b.n);if(chosen.length>8)chosen.pop();}return chosen.map(x=>x.n).sort((a,b)=>a-b)}
function ids(a,b){const z=[];for(let i=1;i<rows.length;i++)if(rows[i].drawDate>=a&&rows[i].drawDate<=b)z.push(i);return z}const sel=ids('2020-01-01','2022-12-31'),mid=ids('2023-01-01','2024-12-31'),late=ids('2025-01-01','9999-12-31');function ev(c,L){let full=0,h5=0,score=0,events=[];for(const i of L){const p=pool(i,c),h=p.filter(n=>sets[i].has(n)).length;score+=h*h*h;if(h===6){full++;events.push({date:rows[i].drawDate,pool:p,truth:[...sets[i]].sort((a,b)=>a-b)})}if(h>=5)h5++;}return{full,h5,score,events}}const cfgs=[];for(const window of [60,120,240,480])for(const alpha of [.1,.25,.5,1])for(const beta of [.05,.1,.25,.5])for(const lambda of [.25,.5,1,2])cfgs.push({window,alpha,beta,lambda});const ranked=cfgs.map((c,id)=>({id,c,sel:ev(c,sel)})).sort((a,b)=>b.sel.full-a.sel.full||b.sel.h5-a.sel.h5||b.sel.score-a.sel.score||a.id-b.id);const frozen=ranked.slice(0,30).map(x=>({...x,mid:ev(x.c,mid),late:ev(x.c,late)}));const summary={configurations:cfgs.length,frozen:frozen.length,midFullModels:frozen.filter(x=>x.mid.full).length,lateFullModels:frozen.filter(x=>x.late.full).length,repeatFullModels:frozen.filter(x=>x.mid.full&&x.late.full).length,repeat5PlusModels:frozen.filter(x=>x.mid.h5&&x.late.h5).length};const out={generatedAt:new Date().toISOString(),engine:'MetaPleno-v99-bonoloto-ising-budget8',methodology:'Pairwise maximum-entropy/Ising-inspired greedy pool selection. Hyperparameters ranked only on 2020-2022; top30 frozen before 2023-2024 and 2025-latest.',budget:{poolSize:8,combinations:28,theoreticalCostEUR:14,maxEUR:15,realStakeEUR:0},summary,leaders:frozen.slice(0,10),realMoneyPass:false};fs.mkdirSync('loterias-ai/data/research',{recursive:true});fs.writeFileSync(OUT,JSON.stringify(out,null,2)+'\n');console.log(JSON.stringify(summary));