import fs from 'node:fs';
const BASE='https://www.loteriasyapuestas.es/es/premios/';
const OUT='loterias-ai/data/research/loteria-nacional-v198-official-sales-geo.json';
const ARCH='loterias-ai/data/archive/loteria-nacional/2026.json';
const months=['enero','febrero','marzo','abril','mayo','junio','julio','agosto','septiembre','octubre','noviembre','diciembre'];
const sleep=ms=>new Promise(r=>setTimeout(r,ms));
const pad=n=>String(n).padStart(2,'0');
function slugDate(iso){const [y,m,d]=iso.split('-').map(Number);return `${pad(d)}-de-${months[m-1]}-de-${y}`}
function candidates(kind,iso){const k=kind==='first'?'primer-premio':'segundo-premio',sd=slugDate(iso);return [
 `${BASE}${k}-de-loteria-nacional-del-sorteo-del-jueves-del-${sd}`,
 `${BASE}${k}-de-loteria-nacional-del-sorteo-de-loteria-nacional-del-${sd}`
]}
function decode(s){return s.replace(/&nbsp;/gi,' ').replace(/&amp;/gi,'&').replace(/&quot;/gi,'"').replace(/&#39;/gi,"'").replace(/&aacute;/gi,'á').replace(/&eacute;/gi,'é').replace(/&iacute;/gi,'í').replace(/&oacute;/gi,'ó').replace(/&uacute;/gi,'ú').replace(/&ntilde;/gi,'ñ').replace(/&Aacute;/g,'Á').replace(/&Eacute;/g,'É').replace(/&Iacute;/g,'Í').replace(/&Oacute;/g,'Ó').replace(/&Uacute;/g,'Ú').replace(/&Ntilde;/g,'Ñ')}
function textOf(html){return decode(html.replace(/<script[\s\S]*?<\/script>/gi,' ').replace(/<style[\s\S]*?<\/style>/gi,' ').replace(/<[^>]+>/g,' ').replace(/\s+/g,' ').trim())}
function parsePoints(html){const t=textOf(html),mark='Punto de Venta:',out=[];let p=0;while((p=t.indexOf(mark,p))>=0){const start=p+mark.length;let end=t.indexOf('Punto de Venta:',start);for(const stop of ['Disponible paraAndroid','Contacto 900','Conócenos']){const z=t.indexOf(stop,start);if(z>=0&&(end<0||z<end))end=z}const raw=t.slice(start,end<0?undefined:end).trim().replace(/\s+/g,' ');if(raw&&!out.includes(raw))out.push(raw);p=start}return out}
async function probe(kind,date){for(const url of candidates(kind,date)){try{const r=await fetch(url,{headers:{'user-agent':'LoteriasAI-research/1.0'}});if(r.status===200){const html=await r.text(),points=parsePoints(html);if(points.length||/PUNTOS DE VENTA RELACIONADOS/i.test(textOf(html)))return{status:'FOUND',url,points}}}catch{}await sleep(150)}return{status:'NOT_FOUND',url:null,points:[]}}
const d=JSON.parse(fs.readFileSync(ARCH,'utf8')),records=(d.records||[]).filter(r=>r.drawDate&&r.drawType!=='EL_NINO'&&r.drawType!=='NAVIDAD').sort((a,b)=>a.drawDate.localeCompare(b.drawDate));
// Probe the latest 24 regular draws first. This is intentionally bounded to avoid hammering SELAE.
const sample=records.slice(-24),rows=[];for(const r of sample){const first=await probe('first',r.drawDate);await sleep(200);const second=await probe('second',r.drawDate);rows.push({drawDate:r.drawDate,drawId:r.official?.drawId||r.drawId,firstPrize:r.result?.firstPrize||null,secondPrize:r.result?.secondPrize||null,first,second});console.log(JSON.stringify({date:r.drawDate,first:first.points.length,second:second.points.length}));await sleep(250)}
const found=rows.filter(x=>x.first.status==='FOUND'||x.second.status==='FOUND'),firstPoints=rows.reduce((s,x)=>s+x.first.points.length,0),secondPoints=rows.reduce((s,x)=>s+x.second.points.length,0);const out={generatedAt:new Date().toISOString(),version:'v198',gameId:'loteria-nacional',provider:'SELAE',sourceClass:'OFFICIAL_PRIZE_SALES_PAGES',scope:{year:2026,regularDrawsProbed:sample.length,boundedProbe:true},summary:{pagesFound:found.reduce((s,x)=>s+(x.first.status==='FOUND')+(x.second.status==='FOUND'),0),drawsWithAnyOfficialSalesGeo:found.length,firstPrizeSalesPoints:firstPoints,secondPrizeSalesPoints:secondPoints},rows,policy:'Only persist explicit SELAE point-of-sale text tied to a prize/date. No inferred geography. Expand backward only after reproducibility audit.',decision:found.length?'OFFICIAL_GEO_SOURCE_CONFIRMED_EXPAND_CAREFULLY':'SOURCE_PATTERN_NOT_REPRODUCED',realMoneyPass:false,realStakeEUR:0};fs.writeFileSync(OUT,JSON.stringify(out,null,2)+'\n');console.log(JSON.stringify(out.summary));