import fs from 'node:fs';import vm from 'node:vm';
const weights={longFrequency:.20,shortMomentum:.15,gap:.15,stability:.15,pairAffinity:.10,entropy:.10,meanReversion:.15};
const ctx={globalThis:{},window:undefined,console,Math,Number,String,Array,Set,Object,Map,Date,JSON};vm.createContext(ctx);vm.runInContext(fs.readFileSync('loterias-ai/js/mega-numeric-engine.js','utf8'),ctx);const M=ctx.globalThis.LotteryMegaNumericEngine;
const url='https://www.azarysuerte.es/HistoricoPR.php';const html=await fetch(url,{headers:{'user-agent':'LoteriasAI/1.0 research','accept':'text/html'}}).then(r=>r.text());
const clean=s=>s.replace(/<[^>]*>/g,' ').replace(/&nbsp;/g,' ').replace(/\s+/g,' ').trim();const money=s=>{if(!s||/BOTE/i.test(s))return 0;return Number(String(s).replace(/,/g,''));};
const anchors=[...html.matchAll(/Premios_colapse\((\d{8})\)/g)].map(m=>({id:m[1],index:m.index}));const prizeRows=[];
for(let ai=0;ai<anchors.length;ai++){const a=anchors[ai],start=Math.max(0,a.index-1800),end=ai+1<anchors.length?anchors[ai+1].index:html.length,block=html.slice(start,end),before=html.slice(start,a.index);const date=`${a.id.slice(0,4)}-${a.id.slice(4,6)}-${a.id.slice(6,8)}`;const mains=[...before.matchAll(/class="ncPR">\s*(\d{1,2})\s*</gi)].map(x=>Number(x[1]));if(mains.length<6)continue;const main=mains.slice(-6);const comps=[...before.matchAll(/class="ncBL">\s*C(?:&nbsp;|\s)*(\d{1,2})/gi)].map(x=>Number(x[1]));const comp=comps.at(-1)??null;const rein=[...before.matchAll(/class="ncPR">\s*R(?:&nbsp;|\s)*(\d)/gi)].map(x=>Number(x[1]));const reintegro=rein.at(-1)??null;const text=clean(block);const get=label=>{const m=text.match(new RegExp(label+'\\s+([0-9,.]+|BOTE)\\s+[0-9,.]+','i'));return m?money(m[1]):0};const prizes={six:get('1ª\\s*\\(6 Aciertos\\)'),fiveC:get('2ª\\s*\\(5 Aciertos \\+ C\\)'),five:get('3ª\\s*\\(5 Aciertos\\)'),four:get('4ª\\s*\\(4 Aciertos\\)'),three:get('5ª\\s*\\(3 Aciertos\\)')};if(main.length===6&&new Set(main).size===6&&comp!=null&&Object.values(prizes).some(x=>x>0))prizeRows.push({date,main,comp,reintegro,prizes});}
const unique=[...new Map(prizeRows.map(r=>[r.date,r])).values()].sort((a,b)=>a.date.localeCompare(b.date));const dir='loterias-ai/data/archive/primitiva';let archive=[];for(const f of fs.readdirSync(dir).filter(f=>/^\d{4}\.json$/.test(f))){archive.push(...(JSON.parse(fs.readFileSync(`${dir}/${f}`,'utf8')).records||[]));}archive=[...new Map(archive.map(r=>[r.drawDate,r])).values()].filter(r=>Array.isArray(r.result?.main)&&r.result.main.length===6).sort((a,b)=>a.drawDate.localeCompare(b.drawDate));
const hit=(ticket,draw)=>ticket.filter(n=>draw.main.includes(n)).length;const payout=(h,ticket,draw)=>{if(h===6)return draw.prizes.six||0;if(h===5){const missing=draw.main.find(n=>!ticket.includes(n));return missing===draw.comp?(draw.prizes.fiveC||0):(draw.prizes.five||0);}if(h===4)return draw.prizes.four||0;if(h===3)return draw.prizes.three||0;return 0};
let stake=0,ret=0,positive=0,total=0;const details=[];for(const draw of unique){const prior=archive.filter(r=>r.drawDate<draw.date).map(r=>r.result.main.map(Number));if(prior.length<250)continue;const p=M.portfolio(prior,{id:'primitiva',main:{max:49,pick:6}},{tickets:5,weights,diversificationPenalty:.10});let rr=0;const tickets=p.tickets.map(t=>{const h=hit(t.numbers,draw),pr=payout(h,t.numbers,draw);rr+=pr;return{numbers:t.numbers,hits:h,prize:pr}});const s=5;stake+=s;ret+=rr;total++;if(rr>s)positive++;details.push({date:draw.date,winning:draw.main,complementary:draw.comp,stake:s,return:rr,pl:rr-s,tickets});}
const out={generatedAt:new Date().toISOString(),gameId:'primitiva',methodology:'locked-balanced-5-ticket-strict-walk-forward-real-main-category-prizes-latest-visible-history',source:url,parsedPrizeDraws:unique.length,drawsEvaluated:total,totalStake:stake,totalReturn:ret,pl:ret-stake,roi:stake?(ret-stake)/stake:null,profitableDrawRate:total?positive/total:null,reintegroTreatment:'EXCLUDED: historical ticket reintegro assignment is unknowable; Especial and Reintegro are excluded, making this conservative for main categories.',ticketsPerDraw:5,ticketCost:1,details};fs.mkdirSync('loterias-ai/data/backtests',{recursive:true});fs.writeFileSync('loterias-ai/data/backtests/primitiva-recent-financial.json',JSON.stringify(out,null,2)+'\n');console.log(JSON.stringify({parsedPrizeDraws:unique.length,draws:total,stake,ret,pl:ret-stake,roi:out.roi,profitableDrawRate:out.profitableDrawRate},null,2));