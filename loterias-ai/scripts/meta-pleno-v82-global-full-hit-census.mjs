import fs from 'node:fs',path from 'node:path',crypto from 'node:crypto';
const ROOT='loterias-ai/data',OUT='loterias-ai/data/research/meta-pleno-v82-global-full-hit-census.json';
const files=[];for(const sub of ['research','backtests','shadow']){const dir=path.join(ROOT,sub);if(!fs.existsSync(dir))continue;for(const f of fs.readdirSync(dir))if(f.endsWith('.json'))files.push(path.join(dir,f))}
const sha=x=>crypto.createHash('sha256').update(JSON.stringify(x)).digest('hex').slice(0,16);function gameOf(file,obj){const s=(file+' '+JSON.stringify({game:obj?.game,gameId:obj?.gameId,engine:obj?.engine})).toLowerCase();if(s.includes('euromill'))return'euromillones';if(s.includes('primitiva'))return'primitiva';if(s.includes('bonoloto'))return'bonoloto';return null}
const raw=[];function walk(v,ctx,depth=0){if(depth>12||v==null)return;if(Array.isArray(v)){for(const x of v)walk(x,ctx,depth+1);return}if(typeof v!=='object')return;const date=v.date||v.drawDate||v.targetDate||null;const hits=Number.isFinite(v.hits)?v.hits:null;const category=typeof v.category==='string'?v.category:null;const pool=v.pool||v.numbers||v.prediction||null;const truth=v.truth||v.result||null;if(date){if((ctx.game==='bonoloto'||ctx.game==='primitiva')&&hits===6)raw.push({...ctx,date,type:'6/6',pool,truth,eventHash:sha({game:ctx.game,date,pool,truth})});if(ctx.game==='euromillones'&&(category==='5+2'||category==='5+2 FULL'||v.mainHits===5&&v.starHits===2))raw.push({...ctx,date,type:'5+2',poolMain:v.poolMain||pool,poolStars:v.poolStars||null,truthMain:v.truthMain||null,truthStars:v.truthStars||null,eventHash:sha({game:ctx.game,date,pool:v.poolMain||pool,stars:v.poolStars})})}for(const [k,x] of Object.entries(v))if(!['truth','result'].includes(k))walk(x,ctx,depth+1)}
for(const file of files){let obj;try{obj=JSON.parse(fs.readFileSync(file,'utf8'))}catch{continue}const game=gameOf(file,obj);if(!game)continue;walk(obj,{game,file,engine:obj.engine||obj.methodology||path.basename(file)},0)}
const uniqueByPrediction=new Map();for(const e of raw){const k=[e.game,e.date,e.type,e.eventHash].join('|');if(!uniqueByPrediction.has(k))uniqueByPrediction.set(k,e)}const unique=[...uniqueByPrediction.values()].sort((a,b)=>a.game.localeCompare(b.game)||a.date.localeCompare(b.date));const byGame={};for(const g of ['bonoloto','primitiva','euromillones']){const ev=unique.filter(x=>x.game===g),dates=[...new Set(ev.map(x=>x.date))];byGame[g]={rawOccurrences:raw.filter(x=>x.game===g).length,dedupedPredictions:ev.length,uniqueFullHitDates:dates.length,dates,events:ev}}
const out={generatedAt:new Date().toISOString(),engine:'MetaPleno-v82-global-full-hit-census',scope:['research','backtests','shadow'],filesScanned:files.length,methodology:'Repository-wide census of persisted explicit full-hit events. Deduplicates identical game/date/prediction events to avoid counting correlated copies. Retrospective census only; does not imply predictability.',byGame,repeatAcrossDistinctDates:{bonoloto:byGame.bonoloto.uniqueFullHitDates>=2,primitiva:byGame.primitiva.uniqueFullHitDates>=2,euromillones:byGame.euromillones.uniqueFullHitDates>=2},realMoneyPass:false};fs.writeFileSync(OUT,JSON.stringify(out,null,2)+'\n');console.log(JSON.stringify({filesScanned:out.filesScanned,summary:Object.fromEntries(Object.entries(byGame).map(([g,x])=>[g,{dates:x.uniqueFullHitDates,predictions:x.dedupedPredictions}]))}));