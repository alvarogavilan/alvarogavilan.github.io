#!/usr/bin/env node
import fs from 'node:fs';import path from 'node:path';const R='loterias-ai';const G=[['bonoloto','bonoloto',49,6,.5,8,'1900-01-01',null],['primitiva','primitiva',49,6,1,4,'1900-01-01',null],['euromillones','euromillones',50,5,2.5,1,'2016-09-27','stars'],['gordo-primitiva','gordo-primitiva',54,5,1.5,2,'1900-01-01','key']].map(([id,dir,max,pick,cost,lines,start,side])=>({id,dir,max,pick,cost,lines,start,side}));
function load(g){let a=[];for(const f of fs.readdirSync(path.join(R,'data','archive',g.dir)).filter(x=>/^\d{4}\.json$/.test(x)).sort()){const j=JSON.parse(fs.readFileSync(path.join(R,'data','archive',g.dir,f)));for(const r of (j.records||j.draws||[])){const date=r.drawDate||r.date,m=(r.result?.main||r.main||r.numbers||[]).map(Number).sort((a,b)=>a-b);if(date&&date>=g.start&&m.length===g.pick)a.push({date,main:m,stars:(r.result?.stars||[]).map(Number),key:Number(r.result?.key)})}}return [...new Map(a.map(x=>[x.date,x])).values()].sort((a,b)=>a.date.localeCompare(b.date))}
function zscores(rows,i,w,g){const recent=rows.slice(Math.max(0,i-w),i),base=rows.slice(Math.max(0,i-4*w),Math.max(0,i-w));const out=[];for(let n=1;n<=g.max;n++){const pr=recent.filter(r=>r.main.includes(n)).length/Math.max(1,recent.length),pb=base.filter(r=>r.main.includes(n)).length/Math.max(1,base.length),p=(pr*recent.length+pb*base.length)/Math.max(1,recent.length+base.length),se=Math.sqrt(Math.max(1e-9,p*(1-p)*(1/Math.max(1,recent.length)+1/Math.max(1,base.length))));out.push([n,(pr-pb)/se])}return out}
function hc(z){const ps=z.map(x=>Math.exp(-.5*x[1]*x[1])).sort((a,b)=>a-b),m=ps.length;let h=0;for(let i=0;i<m;i++){const p=ps[i],q=(i+1)/m;if(p>.001&&p<.5)h=Math.max(h,Math.sqrt(m)*(q-p)/Math.sqrt(p*(1-p)))}return h}
function combos(a,k){const o=[];function r(s,c){if(c.length===k){o.push([...c]);return}for(let i=s;i<=a.length-(k-c.length);i++){c.push(a[i]);r(i+1,c);c.pop()}}r(0,[]);return o}
function side(g,rows,i,w){if(g.side==='stars'){const c=Array(13).fill(0);for(const r of rows.slice(Math.max(0,i-w),i))for(const s of r.stars||[])c[s]++;return [...Array(12)].map((_,i)=>i+1).sort((a,b)=>c[b]-c[a]||a-b).slice(0,2)}if(g.side==='key'){const c=Array(10).fill(0);for(const r of rows.slice(Math.max(0,i-w),i))if(Number.isInteger(r.key)&&r.key>=0&&r.key<=9)c[r.key]++;return [...Array(10)].map((_,i)=>i).sort((a,b)=>c[b]-c[a]||a-b)[0]}return null}
function evalEra(g,rows,c,s,e){let d=0,near=0,full=0,jack=0,sum=0;for(let i=1;i<rows.length;i++){if(rows[i].date<s||rows[i].date>=e||i<4*c.w)continue;const z=zscores(rows,i,c.w,g),H=hc(z);if(H<c.thr)continue;const rank=z.sort((a,b)=>b[1]-a[1]||a[0]-b[0]).map(x=>x[0]),ls=combos(rank.slice(0,g.pick+2),g.pick).slice(0,g.lines),truth=new Set(rows[i].main);let best=0;for(const l of ls)best=Math.max(best,l.filter(x=>truth.has(x)).length);d++;sum+=best;if(best>=g.pick-1)near++;if(best===g.pick){full++;if(!g.side)jack++;else if(g.side==='stars'){const p=side(g,rows,i,c.w);if(p.length===2&&p.every(x=>rows[i].stars.includes(x)))jack++}else if(side(g,rows,i,c.w)===rows[i].key)jack++}}return{draws:d,meanBestHits:d?sum/d:0,nearFull:near,fullMain:full,fullJackpot:jack}}
const games=[];for(const g of G){const rows=load(g),cfg=[];for(const w of [40,80,160]){const hs=[];for(let i=1;i<rows.length;i++)if(rows[i].date>='2019-01-01'&&rows[i].date<'2023-01-01'&&i>=4*w)hs.push(hc(zscores(rows,i,w,g)));hs.sort((a,b)=>a-b);for(const q of [.75,.9,.95])cfg.push({w,q,thr:hs[Math.floor((hs.length-1)*q)]??Infinity})}let r=cfg.map(c=>({c,selection:evalEra(g,rows,c,'2019-01-01','2023-01-01')})).sort((a,b)=>b.selection.fullJackpot-a.selection.fullJackpot||b.selection.fullMain-a.selection.fullMain||b.selection.nearFull-a.selection.nearFull||b.selection.meanBestHits-a.selection.meanBestHits).slice(0,4).map(x=>({...x,validation:evalEra(g,rows,x.c,'2023-01-01','2025-01-01'),postFreeze:evalEra(g,rows,x.c,'2025-01-01','9999-12-31')}));const sig=r.some(x=>x.validation.nearFull>0&&x.postFreeze.nearFull>0)||r.some(x=>x.validation.fullJackpot>0||x.postFreeze.fullJackpot>0);games.push({gameId:g.id,archiveDraws:rows.length,budget:{lineCostEUR:g.cost,lines:g.lines,theoreticalEUR:g.cost*g.lines},leaders:r,decision:sig?'AUDIT_SIGNAL':'NEGATIVE'})}
const out={version:'v283',family:'CONSOLIDATED_HIGHER_CRITICISM_SPARSE_DRIFT',generatedAt:new Date().toISOString(),methodology:'Sparse distribution-shift detector using Higher-Criticism-style aggregation of recent-vs-baseline number-frequency deviations. Hyperparameters and gate quantiles selected only on 2019-2022, frozen for untouched 2023-2024 and 2025+. Exact stars/Clave; compact budgets <=4 EUR.',games,anyAuditSignal:games.some(g=>g.decision==='AUDIT_SIGNAL'),realMoneyPass:false,realStakeEUR:0,computePolicy:{singleActionForFourGames:true}};fs.mkdirSync(path.join(R,'data','research'),{recursive:true});fs.writeFileSync(path.join(R,'data','research','metapleno-v283-consolidated-higher-criticism.json'),JSON.stringify(out,null,2)+'\n');console.log(JSON.stringify({anyAuditSignal:out.anyAuditSignal,games:games.map(g=>({gameId:g.gameId,decision:g.decision,budget:g.budget}))},null,2));