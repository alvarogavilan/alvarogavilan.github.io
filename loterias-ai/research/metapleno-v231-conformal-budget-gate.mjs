#!/usr/bin/env node
/** v231 — Conformal Budget Gate. Fail-closed budget selector trained only pre-2023. */
import fs from 'node:fs';
import path from 'node:path';
const root=path.resolve('loterias-ai');
function loadArchive(){const dir=path.join(root,'data','archive','bonoloto');const files=fs.readdirSync(dir).filter(f=>/^\d{4}\.json$/.test(f)).sort();let rows=[];for(const f of files){const x=JSON.parse(fs.readFileSync(path.join(dir,f),'utf8'));rows.push(...(x.records||x.draws||[]));}return [...new Map(rows.map(x=>[x.drawId||`${x.drawDate||x.date}|${JSON.stringify(x.result?.main||x.main||x.numbers)}`,x])).values()];}
const rec=loadArchive().map(x=>({date:x.drawDate||x.date,main:(x.result?.main||x.main||x.numbers||[]).map(Number)})).filter(x=>x.date&&x.main.length===6&&x.main.every(n=>n>=1&&n<=49)).sort((a,b)=>a.date.localeCompare(b.date));
const train=rec.filter(x=>x.date<'2023-01-01'),val=rec.filter(x=>x.date>='2023-01-01'&&x.date<'2025-01-01'),post=rec.filter(x=>x.date>='2025-01-01');
function rank(hist){const s=Array(50).fill(0),last=Array(50).fill(-9999);hist.forEach((d,i)=>d.main.forEach(n=>{s[n]++;last[n]=i}));return Array.from({length:49},(_,i)=>i+1).sort((a,b)=>(s[b]+.12*(hist.length-last[b]))-(s[a]+.12*(hist.length-last[a]))||a-b)}
function evalEra(era,history,k){let n=0,h5=0,h6=0;const rows=[];for(const d of era){const r=rank(history),set=new Set(r.slice(0,k)),h=d.main.filter(x=>set.has(x)).length;n++;if(h>=5)h5++;if(h===6)h6++;rows.push({date:d.date,hits:h});history.push(d)}return{n,h5,h6,rows}}
const alpha=.20,ks=[6,7,8,9],cal=train.filter(x=>x.date>='2018-01-01');let selected=9,calibration=[];
for(const k of ks){const h=train.filter(x=>x.date<'2018-01-01');const e=evalEra(cal,h,k);const miss=1-e.h5/Math.max(1,e.n);calibration.push({k,n:e.n,hit5:e.h5,missRate:miss});if(miss<=alpha){selected=k;break}}
const budget=selected<=8?14:15;const validation=evalEra(val,train.slice(),selected),postFreeze=evalEra(post,train.concat(val),selected);
const result={version:'v231',engine:'Conformal Budget Gate',game:'bonoloto',generatedAt:new Date().toISOString(),candidatePool:{nominalConfigurations:ks.length,effectiveConfigurations:ks.length,selectionCriterion:'Smallest pre-2023 calibrated pool size with >=5 miss-rate <= alpha; otherwise fail to k=9 research-only.'},dataBoundary:{train:'<2023-01-01',validation:'2023-2024',postFreeze:'2025+'},selection:{trainEnd:'2022-12-31',alpha,selectedPoolSize:selected,theoreticalBudgetEUR:budget,realMoneyPass:false,realStakeEUR:0},calibration,validation:{n:validation.n,hit5:validation.h5,hit6:validation.h6},postFreeze:{n:postFreeze.n,hit5:postFreeze.h5,hit6:postFreeze.h6},decision:'RESEARCH_ONLY',note:'Calibration is pre-2023 only. A pool-size gate is not a lottery edge and cannot authorize real money.'};
const out=path.join(root,'data','research','metapleno-v231-conformal-budget-gate.json');fs.mkdirSync(path.dirname(out),{recursive:true});fs.writeFileSync(out,JSON.stringify(result,null,2)+'\n');console.log(JSON.stringify(result,null,2));