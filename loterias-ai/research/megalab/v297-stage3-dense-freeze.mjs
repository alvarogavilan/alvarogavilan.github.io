#!/usr/bin/env node
import fs from'node:fs';import path from'node:path';import crypto from'node:crypto';
const R='loterias-ai',IN=path.join(R,'data/research/metapleno-v297-stage1-2-selection.json'),OUT=path.join(R,'data/research/metapleno-v297-stage3-dense-freeze.json'),MAN=path.join(R,'data/research/metapleno-v297-frozen-manifest.json');const H=s=>crypto.createHash('sha256').update(String(s)).digest('hex');
const G={bonoloto:{dir:'bonoloto',max:49,pick:6},primitiva:{dir:'primitiva',max:49,pick:6},euromillones:{dir:'euromillones',max:50,pick:5},'gordo-primitiva':{dir:'gordo-primitiva',max:54,pick:5}};
function load(id){const g=G[id],a=[];for(const f of fs.readdirSync(path.join(R,'data/archive',g.dir)).filter(x=>/^\d{4}\.json$/.test(x)).sort()){const j=JSON.parse(fs.readFileSync(path.join(R,'data/archive',g.dir,f)));for(const z of j.records||j.draws||[]){const date=z.drawDate||z.date,main=(z.result?.main||z.main||z.numbers||[]).map(Number).sort((a,b)=>a-b);if(date&&date>='2019-01-01'&&date<'2023-01-01'&&main.length===g.pick)a.push({date,main})}}return[...new Map(a.map(x=>[x.date,x])).values()].sort((a,b)=>a.date.localeCompare(b.date))}
function rnd(seed,i){let x=(seed*1664525+1013904223+i*2654435761)>>>0;x^=x<<13;x^=x>>>17;x^=x<<5;return((x>>>0)/4294967296)*2-1}
function feat(set,g,e,j){if(e==='normalized-set-indicator')return set.includes((j%g.max)+1)?1:-.12;if(e==='gap-vector-padded'){const gaps=[set[0]-1];for(let i=1;i<set.length;i++)gaps.push(set[i]-set[i-1]-1);gaps.push(g.max-set.at(-1));return ((gaps[j%gaps.length]||0)/g.max)*2-.5}if(e==='residue-count-state'){const m=[2,3,5,7][j%4],r=Math.abs(j)%m;return set.filter(x=>x%m===r).length/set.length-.25}if(e==='pair-distance-moments'){const d=[];for(let a=0;a<set.length;a++)for(let b=a+1;b<set.length;b++)d.push((set[b]-set[a])/g.max);const mean=d.reduce((a,b)=>a+b,0)/Math.max(1,d.length),v=d.reduce((a,b)=>a+(b-mean)**2,0)/Math.max(1,d.length);return j%2?Math.sqrt(v):mean}const sum=set.reduce((a,b)=>a+b,0)/(g.max*set.length),span=(set.at(-1)-set[0])/g.max,par=set.filter(x=>x%2===0).length/set.length;return[sum,span,par][j%3]-.5}
function step(state,set,g,s){const out=new Float64Array(s.size);for(let j=0;j<s.size;j++){const ring=state[(j-1+s.size)%s.size],inp=feat(set,g,s.encoding,j)*rnd(s.seed,j),raw=s.radius*ring+.9*inp+.08*rnd(s.seed+97,j);out[j]=(1-s.leak)*state[j]+s.leak*Math.tanh(raw)}return out}
function predictAt(rows,idx,g,s){const start=Math.max(0,idx-65),num=g.max,N=s.size;let state=new Float64Array(N);const xy=Array.from({length:num},()=>new Float64Array(N)),xx=new Float64Array(N);for(let t=start;t<idx-1;t++){state=step(state,rows[t].main,g,s);for(let j=0;j<N;j++){const z=state[j];xx[j]+=z*z;for(const n of rows[t+1].main)xy[n-1][j]+=z}}state=step(state,rows[idx-1].main,g,s);const scores=[];for(let n=1;n<=num;n++){let sc=0;for(let j=0;j<N;j++)sc+=(xy[n-1][j]/(s.ridge+xx[j]))*state[j];scores.push([sc+1e-9*rnd(s.seed+idx,n),n])}scores.sort((a,b)=>b[0]-a[0]||a[1]-b[1]);return scores.slice(0,g.pick).map(x=>x[1]).sort((a,b)=>a-b)}
const src=JSON.parse(fs.readFileSync(IN));const byGame={},frozen=[];for(const[id,g]of Object.entries(G)){const rows=load(id),leaders=(src.byGame[id]?.best||[]).slice(0,48),seen=new Map();for(const s of leaders){let hits=0,near=0,full=0,n=0;const hist={},traj=[];for(let i=80;i<rows.length;i++){const p=predictAt(rows,i,g,s),k=p.filter(x=>rows[i].main.includes(x)).length;traj.push(p.join('-'));hits+=k;n++;hist[k]=(hist[k]||0)+1;if(k>=g.pick-1)near++;if(k===g.pick)full++}const behaviorHash=H(traj.join('|')),rec={scientistId:s.scientistId,game:id,encoding:s.encoding,size:s.size,radius:s.radius,leak:s.leak,ridge:s.ridge,seed:s.seed,behaviorHash,selection:{draws:n,meanHits:hits/Math.max(1,n),nearFull:near,fullMain:full,hitHistogram:hist}};const old=seen.get(behaviorHash);if(!old||full>old.selection.fullMain||near>old.selection.nearFull||hits/n>old.selection.meanHits)seen.set(behaviorHash,rec)}const reps=[...seen.values()].sort((a,b)=>b.selection.fullMain-a.selection.fullMain||b.selection.nearFull-a.selection.nearFull||b.selection.meanHits-a.selection.meanHits||a.behaviorHash.localeCompare(b.behaviorHash));const chosen=reps.slice(0,8);frozen.push(...chosen);byGame[id]={inputLeaders:leaders.length,behavioralRepresentatives:reps.length,behavioralDuplicates:leaders.length-reps.length,nearFullBehaviors:reps.filter(x=>x.selection.nearFull>0).length,fullMainBehaviors:reps.filter(x=>x.selection.fullMain>0).length,frozen:chosen}}
const slim=frozen.map(x=>({scientistId:x.scientistId,game:x.game,encoding:x.encoding,size:x.size,radius:x.radius,leak:x.leak,ridge:x.ridge,seed:x.seed,behaviorHash:x.behaviorHash})),manifestHash=H(JSON.stringify(slim));const manifest={version:'v297-freeze',family:'RESERVOIR_COMPUTING_ECHO_STATE',frozenAt:new Date().toISOString(),selectionWindow:'2019-01-01/2023-01-01',candidateCount:slim.length,manifestHash,candidates:slim,validationTouched:false,postFreezeTouched:false,retuningPerformed:false,realMoneyPass:false,realStakeEUR:0};const out={version:'v297-stage3',family:'RESERVOIR_COMPUTING_ECHO_STATE',generatedAt:new Date().toISOString(),candidateCount:slim.length,manifestHash,byGame,validationTouched:false,postFreezeTouched:false,retuningPerformed:false,realMoneyPass:false,realStakeEUR:0,next:'strict-oos-2023-2024'};fs.writeFileSync(MAN,JSON.stringify(manifest,null,2)+'\n');fs.writeFileSync(OUT,JSON.stringify(out,null,2)+'\n');console.log(JSON.stringify({candidateCount:slim.length,manifestHash,byGame:Object.fromEntries(Object.entries(byGame).map(([k,v])=>[k,{reps:v.behavioralRepresentatives,near:v.nearFullBehaviors,full:v.fullMainBehaviors,frozen:v.frozen.length}])),validationTouched:false,realMoneyPass:false},null,2));