#!/usr/bin/env node
import fs from 'node:fs';
import path from 'node:path';
import crypto from 'node:crypto';
const ROOT='loterias-ai',FREEZE=`${ROOT}/data/research/metapleno-v319-validation-freeze.json`,SPEC=`${ROOT}/data/research/metapleno-v319-preregister.json`,OUT=`${ROOT}/data/research/metapleno-v319-stage3-validation.json`;
if(!fs.existsSync(FREEZE))throw new Error('v319 validation freeze not yet persisted');
const raw=fs.readFileSync(FREEZE,'utf8'),freeze=JSON.parse(raw),spec=JSON.parse(fs.readFileSync(SPEC,'utf8'));
if(freeze.status!=='FROZEN_BEFORE_VALIDATION_READ'||freeze.temporalCustody?.validationReadBeforeFreeze!==false||freeze.temporalCustody?.blindOosReadBeforeFreeze!==false)throw new Error('validation custody invalid');
const {manifestHash,generatedAt,...core}=freeze;if(crypto.createHash('sha256').update(JSON.stringify(core)).digest('hex')!==manifestHash)throw new Error('freeze manifest mismatch');
if(freeze.guards?.parametersFrozen!==true||freeze.guards?.retuningAllowed!==false||freeze.guards?.realMoneyPass!==false)throw new Error('freeze safety drift');
const START='2019-01-01',END='2022-12-31',MAXD=6;
function choose(n,k){if(k<0||k>n)return 0;k=Math.min(k,n-k);let x=1;for(let i=1;i<=k;i++)x=x*(n-k+i)/i;return x;}
const P5=(choose(6,5)*choose(43,3)+choose(6,6)*choose(43,2))/choose(49,8),P6=choose(43,2)/choose(49,8);
function load(game){const dir=`${ROOT}/data/archive/${game}`,files=fs.readdirSync(dir).filter(f=>/^\d{4}\.json$/.test(f)&&Number(f.slice(0,4))<=2022).sort();let rows=[];for(const f of files){if(Number(f.slice(0,4))>2022)throw new Error('2023+ file opened');const d=JSON.parse(fs.readFileSync(path.join(dir,f),'utf8'));for(const r of (Array.isArray(d)?d:(d.records||[]))){const date=String(r.drawDate||'').slice(0,10),m=(r.result?.main||[]).map(Number);if(date&&date<=END&&m.length===6&&new Set(m).size===6&&m.every(n=>n>=1&&n<=49))rows.push({date,main:m});}}rows.sort((a,b)=>a.date.localeCompare(b.date));return{rows,openedFiles:files};}
const cache={};for(const g of new Set(freeze.candidates.map(c=>c.game)))cache[g]=load(g);
function bitsFrom(rows){const b=Array.from({length:50},()=>new Uint8Array(rows.length));for(let i=0;i<rows.length;i++)for(const n of rows[i].main)b[n][i]=1;return b;}
function ctx(bits,n,y,d){let c=0;for(let j=y-d;j<y;j++)c=(c<<1)|bits[n][j];return c;}
function weights(D){const w=[];let s=0;for(let d=0;d<=D;d++){const x=2**(-d);w.push(x);s+=x;}return w.map(x=>x/s);}
function top8(scores){return scores.slice(1).map((s,i)=>({n:i+1,s})).sort((a,b)=>b.s-a.s||a.n-b.n).slice(0,8).map(x=>x.n).sort((a,b)=>a-b);}
function hit(pool,win){const s=new Set(pool);return win.reduce((z,n)=>z+s.has(n),0);}
function evalModel(game,W,D){const rows=cache[game].rows,bits=bitsFrom(rows),metrics={evaluated:0,five:0,six:0,sum:0,events:[],hash:crypto.createHash('sha256')};for(let t=0;t<rows.length;t++){const date=rows[t].date;if(date<START||date>END)continue;if(t<W||t<MAXD)continue;const counts=Array.from({length:D+1},(_,d)=>Array.from({length:50},()=>new Uint32Array((1<<d)*2)));for(let d=0;d<=D;d++)for(let y=Math.max(d,t-W);y<t;y++)for(let n=1;n<=49;n++){const c=d?ctx(bits,n,y,d):0,o=bits[n][y];counts[d][n][c*2+o]++;}const w=weights(D),scores=Array(50).fill(0);for(let n=1;n<=49;n++){let p=0;for(let d=0;d<=D;d++){const c=d?ctx(bits,n,t,d):0,a=counts[d][n],z=a[c*2]+a[c*2+1],pd=(a[c*2+1]+.5)/(z+1);p+=w[d]*pd;}scores[n]=p;}const pool=top8(scores),h=hit(pool,rows[t].main);metrics.evaluated++;metrics.sum+=h;metrics.hash.update(`${date}:${pool.join(',')}\n`);if(h>=5){metrics.five++;metrics.events.push({date,hits:h,pool,winning:rows[t].main});}if(h===6)metrics.six++;}return{evaluatedDraws:metrics.evaluated,fivePlusPoolHits:metrics.five,fullSixPoolHits:metrics.six,meanPoolHits:metrics.evaluated?metrics.sum/metrics.evaluated:0,behaviorHash:metrics.hash.digest('hex'),events:metrics.events,matchedRandomExpectedFivePlus:metrics.evaluated*P5,matchedRandomExpectedFullSix:metrics.evaluated*P6};}
const baselineCache=new Map(),results=[];for(const c of freeze.candidates){const key=`${c.game}|${c.window}`,base=baselineCache.get(key)||evalModel(c.game,Number(c.window),0);baselineCache.set(key,base);const cand=evalModel(c.game,Number(c.window),Number(c.maxOrder)),dates=[...new Set(cand.events.map(e=>e.date))],excess0=cand.fivePlusPoolHits-base.fivePlusPoolHits,excessRnd=cand.fivePlusPoolHits-cand.matchedRandomExpectedFivePlus,pass=excess0>0&&excessRnd>0&&dates.length>=Number(spec.validationGate.minimumDistinctFivePlusDates);results.push({game:c.game,window:Number(c.window),maxOrder:Number(c.maxOrder),developmentBehaviorHash:c.developmentBehaviorHash,...cand,order0:{fivePlusPoolHits:base.fivePlusPoolHits,fullSixPoolHits:base.fullSixPoolHits,meanPoolHits:base.meanPoolHits,behaviorHash:base.behaviorHash},fivePlusExcessOverOrder0:excess0,fivePlusExcessOverRandom:excessRnd,distinctFivePlusDates:dates,validationGatePass:pass});}
const survivors=results.filter(r=>r.validationGatePass).map(({events,...r})=>({...r,eventDetails:events}));const out={generatedAt:new Date().toISOString(),version:'v319-stage3-validation',family:freeze.family,status:survivors.length?'VALIDATION_SURVIVORS_READY_FOR_BLIND_OOS_FREEZE':'CLOSED_VALIDATION_FAILED',freezeManifestHash:freeze.manifestHash,temporalIsolation:{validationWindow:`${START}/${END}`,openedFiles:Object.fromEntries(Object.entries(cache).map(([g,v])=>[g,v.openedFiles])),blindOosTouched:false,postFreezeTouched:false},exactMatchedNull:{fivePlusProbability:P5,fullSixProbability:P6},candidateCount:freeze.candidates.length,results:results.map(({events,...r})=>({...r,eventDates:events.map(e=>e.date)})),survivors,survivorCount:survivors.length,guards:{parametersChangedAfterFreeze:false,retuningPerformed:false,realMoneyPass:false,realStakeEUR:0,maximumTheoreticalCostEURPerGameDraw:14},next:survivors.length?'freeze survivors before reading 2023+':'archive v319; do not read 2023+ OOS'};fs.writeFileSync(OUT,JSON.stringify(out,null,2)+'\n');console.log(JSON.stringify({status:out.status,survivorCount:out.survivorCount,survivors:survivors.map(x=>({game:x.game,window:x.window,maxOrder:x.maxOrder,fivePlus:x.fivePlusPoolHits,order0:x.order0.fivePlusPoolHits,randomExpected:x.matchedRandomExpectedFivePlus,dates:x.distinctFivePlusDates,fullSix:x.fullSixPoolHits})),blindOosTouched:false,realMoneyPass:false},null,2));
