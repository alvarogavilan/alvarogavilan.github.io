name: Loterias AI Primitiva Holdout
on:
  push:
    paths:
      - 'loterias-ai/scripts/primitiva-holdout-evidence.mjs'
      - '.github/workflows/loterias-ai-primitiva-holdout.yml'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  holdout:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node loterias-ai/scripts/primitiva-holdout-evidence.mjs
      - name: Persist result safely
        run: |
          cp loterias-ai/data/backtests/primitiva-holdout.json /tmp/primitiva-holdout.json
          git fetch origin main
          git reset --hard origin/main
          cp /tmp/primitiva-holdout.json loterias-ai/data/backtests/primitiva-holdout.json
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add loterias-ai/data/backtests/primitiva-holdout.json
          git commit -m 'data(loterias-ai): refresh Primitiva locked holdout' || exit 0
          git push origin HEAD:main