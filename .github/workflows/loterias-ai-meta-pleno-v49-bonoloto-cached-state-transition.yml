name: Loterias AI MetaPleno v49 Bonoloto Cached State Transition
on:
  workflow_dispatch:
  push:
    paths:
      - 'loterias-ai/scripts/meta-pleno-v49-bonoloto-cached-state-transition.mjs'
      - '.github/workflows/loterias-ai-meta-pleno-v49-bonoloto-cached-state-transition.yml'
permissions:
  contents: write
concurrency:
  group: loterias-ai-meta-pleno-v49-bonoloto-cached-state-transition
  cancel-in-progress: false
jobs:
  research:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with: {fetch-depth: 0}
      - uses: actions/setup-node@v4
        with: {node-version: 22}
      - name: Run cached state-transition search
        run: node loterias-ai/scripts/meta-pleno-v49-bonoloto-cached-state-transition.mjs
      - name: Persist result safely
        run: |
          cp loterias-ai/data/research/meta-pleno-v49-bonoloto-cached-state-transition.json /tmp/v49.json
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          for i in 1 2 3 4 5; do
            git fetch origin main
            git reset --hard origin/main
            cp /tmp/v49.json loterias-ai/data/research/meta-pleno-v49-bonoloto-cached-state-transition.json
            git add loterias-ai/data/research/meta-pleno-v49-bonoloto-cached-state-transition.json
            git commit -m 'data(loterias-ai): persist MetaPleno v49 Bonoloto state-transition result' || exit 0
            git push origin HEAD:main && exit 0
            sleep $((i*3))
          done
          exit 1
