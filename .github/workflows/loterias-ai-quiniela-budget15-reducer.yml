name: Loterias AI Quiniela Budget15 Reducer
on:
  workflow_dispatch:
  push:
    branches: [research/quiniela-budget15-reducer]
    paths:
      - 'loterias-ai/scripts/quiniela-budget15-reducer.mjs'
      - '.github/workflows/loterias-ai-quiniela-budget15-reducer.yml'
      - 'loterias-ai/data/shadow/quiniela-current-combination.json'
jobs:
  reduce:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: {node-version: 22}
      - run: node loterias-ai/scripts/quiniela-budget15-reducer.mjs
      - name: Persist reduced system
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add loterias-ai/data/shadow/quiniela-current-budget15.json
          git diff --cached --quiet || git commit -m 'data(loterias-ai): persist Quiniela Budget15 reduction'
          git push
