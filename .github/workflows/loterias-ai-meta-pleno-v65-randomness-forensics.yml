name: Loterias AI MetaPleno v65 Randomness Forensics
on:
  workflow_dispatch:
  push:
    paths:
      - 'loterias-ai/scripts/meta-pleno-v65-randomness-forensics.mjs'
      - '.github/workflows/loterias-ai-meta-pleno-v65-randomness-forensics.yml'
permissions:
  contents: write
jobs:
  audit:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        game: [bonoloto, primitiva, euromillones]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Run randomness forensics
        env:
          GAME: ${{ matrix.game }}
        run: node loterias-ai/scripts/meta-pleno-v65-randomness-forensics.mjs
      - name: Persist result safely
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git pull --rebase origin main
          git add loterias-ai/data/research/meta-pleno-v65-${{ matrix.game }}-randomness-forensics.json
          git diff --cached --quiet && exit 0
          git commit -m "data(loterias-ai): persist v65 ${{ matrix.game }} randomness audit"
          for i in 1 2 3 4 5; do
            git push origin HEAD:main && exit 0
            git pull --rebase origin main
          done
          exit 1
