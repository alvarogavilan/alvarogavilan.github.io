name: Loterias AI MetaPleno v5 Repeatability
on:
  workflow_dispatch:
  push:
    paths:
      - 'loterias-ai/scripts/meta-pleno-v5-repeatability.mjs'
      - 'loterias-ai/scripts/meta-pleno-v5-aggregate.mjs'
      - '.github/workflows/loterias-ai-meta-pleno-v5-repeatability.yml'
permissions:
  contents: write
concurrency:
  group: loterias-ai-meta-pleno-v5-repeatability
  cancel-in-progress: true
jobs:
  search:
    strategy:
      fail-fast: false
      matrix:
        game: [primitiva, bonoloto, euromillones, eurodreams, gordo-primitiva]
        shard: [0,1,2,3]
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: {node-version: 22}
      - name: Search 50k repeatability-first formulas
        env:
          GAME: ${{ matrix.game }}
          SHARD: ${{ matrix.shard }}
          CANDIDATES: 50000
        run: node loterias-ai/scripts/meta-pleno-v5-repeatability.mjs
      - uses: actions/upload-artifact@v4
        with:
          name: meta-pleno-v5-${{ matrix.game }}-${{ matrix.shard }}
          path: loterias-ai/data/research/meta-pleno-v5-shards/${{ matrix.game }}-${{ matrix.shard }}.json
  aggregate:
    needs: search
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: {fetch-depth: 0}
      - uses: actions/setup-node@v4
        with: {node-version: 22}
      - uses: actions/download-artifact@v4
        with:
          pattern: meta-pleno-v5-*
          path: /tmp/meta-v5
      - name: Collect frozen finalists
        run: |
          rm -rf loterias-ai/data/research/meta-pleno-v5-shards
          mkdir -p loterias-ai/data/research/meta-pleno-v5-shards
          find /tmp/meta-v5 -name '*.json' -exec cp {} loterias-ai/data/research/meta-pleno-v5-shards/ \;
          test "$(find loterias-ai/data/research/meta-pleno-v5-shards -name '*.json' | wc -l)" -eq 20
      - name: Open final test years
        run: node loterias-ai/scripts/meta-pleno-v5-aggregate.mjs
      - name: Persist safely
        run: |
          cp loterias-ai/data/research/meta-pleno-v5-repeatability.json /tmp/v5.json
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          for i in 1 2 3 4 5; do
            git fetch origin main
            git reset --hard origin/main
            cp /tmp/v5.json loterias-ai/data/research/meta-pleno-v5-repeatability.json
            git add loterias-ai/data/research/meta-pleno-v5-repeatability.json
            git commit -m 'data(loterias-ai): persist MetaPleno v5 repeatability evidence' || exit 0
            git push origin HEAD:main && exit 0
            sleep $((i*3))
          done
          exit 1
