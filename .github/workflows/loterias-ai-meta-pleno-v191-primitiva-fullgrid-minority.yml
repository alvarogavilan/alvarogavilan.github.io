name: Loterias AI MetaPleno v191 Primitiva Full Grid Minority
on:
  workflow_dispatch:
  push:
    branches: [research/metapleno-v191-primitiva-fullgrid-minority]
permissions:
  contents: write
jobs:
  research:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: node loterias-ai/scripts/meta-pleno-v191-primitiva-fullgrid-minority.mjs
      - name: Persist v191
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add loterias-ai/data/research/meta-pleno-v191-primitiva-fullgrid-minority.json
          if ! git diff --cached --quiet; then
            git commit -m "research(loterias-ai): persist v191 full-grid minority"
            git pull --rebase origin "$GITHUB_REF_NAME"
            git push origin HEAD:"$GITHUB_REF_NAME"
          fi
