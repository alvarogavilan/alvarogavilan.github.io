name: Loterias AI MetaPleno v32 Cross Game Local Robustness
on:
  workflow_dispatch:
  push:
    paths:
      - 'loterias-ai/scripts/meta-pleno-v32-cross-game-local-robustness.mjs'
      - '.github/workflows/loterias-ai-meta-pleno-v32-cross-game-local-robustness.yml'
permissions:
  contents: write
jobs:
  evaluate:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with: {fetch-depth: 0}
      - uses: actions/setup-node@v4
        with: {node-version: 22}
      - name: Run 15k local perturbations per game
        env: {N: 15000}
        run: node loterias-ai/scripts/meta-pleno-v32-cross-game-local-robustness.mjs
      - name: Persist safely
        run: |
          cp loterias-ai/data/research/meta-pleno-v32-cross-game-local-robustness.json /tmp/v32.json
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          for i in 1 2 3 4 5; do
            git fetch origin main
            git reset --hard origin/main
            cp /tmp/v32.json loterias-ai/data/research/meta-pleno-v32-cross-game-local-robustness.json
            git add loterias-ai/data/research/meta-pleno-v32-cross-game-local-robustness.json
            git commit -m 'data(loterias-ai): persist MetaPleno v32 cross-game robustness' || exit 0
            git push origin HEAD:main && exit 0
            sleep $((i*2))
          done
          exit 1
