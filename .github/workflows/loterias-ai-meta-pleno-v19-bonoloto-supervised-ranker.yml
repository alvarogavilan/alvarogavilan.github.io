name: Loterias AI MetaPleno v19 Bonoloto Supervised Ranker
on:
  push:
    paths:
      - 'loterias-ai/scripts/meta-pleno-v19-bonoloto-supervised-ranker.mjs'
      - '.github/workflows/loterias-ai-meta-pleno-v19-bonoloto-supervised-ranker.yml'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  evaluate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node loterias-ai/scripts/meta-pleno-v19-bonoloto-supervised-ranker.mjs
      - name: Persist safely
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add loterias-ai/data/research/meta-pleno-v19-bonoloto-supervised-ranker.json
          git commit -m 'data(loterias-ai): persist Bonoloto v19 supervised ranker' || exit 0
          git pull --rebase origin main
          git push origin HEAD:main
