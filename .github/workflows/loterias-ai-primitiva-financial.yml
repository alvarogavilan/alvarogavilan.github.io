name: Loterias AI Primitiva Financial
on:
  push:
    paths:
      - 'loterias-ai/scripts/primitiva-recent-financial-backtest.mjs'
      - '.github/workflows/loterias-ai-primitiva-financial.yml'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  financial:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node loterias-ai/scripts/primitiva-recent-financial-backtest.mjs
      - name: Persist result safely
        run: |
          cp loterias-ai/data/backtests/primitiva-recent-financial.json /tmp/primitiva-recent-financial.json
          git fetch origin main
          git reset --hard origin/main
          cp /tmp/primitiva-recent-financial.json loterias-ai/data/backtests/primitiva-recent-financial.json
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add loterias-ai/data/backtests/primitiva-recent-financial.json
          git commit -m 'data(loterias-ai): refresh Primitiva financial backtest' || exit 0
          git push origin HEAD:main