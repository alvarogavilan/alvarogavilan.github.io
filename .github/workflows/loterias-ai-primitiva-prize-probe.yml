name: Loterias AI Primitiva Prize Probe
on:
  push:
    paths:
      - 'loterias-ai/scripts/probe-primitiva-prize-source.mjs'
      - '.github/workflows/loterias-ai-primitiva-prize-probe.yml'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node loterias-ai/scripts/probe-primitiva-prize-source.mjs
      - name: Persist result safely
        run: |
          cp loterias-ai/data/probes/primitiva-prize-source.json /tmp/primitiva-prize-source.json
          git fetch origin main
          git reset --hard origin/main
          cp /tmp/primitiva-prize-source.json loterias-ai/data/probes/primitiva-prize-source.json
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add loterias-ai/data/probes/primitiva-prize-source.json
          git commit -m 'data(loterias-ai): refresh Primitiva prize-source probe' || exit 0
          git push origin HEAD:main