name: Loterias AI Summarize MetaPleno v12
on:
  push:
    paths:
      - 'loterias-ai/scripts/summarize-meta-pleno-v12.mjs'
permissions:
  contents: write
jobs:
  summarize:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: {fetch-depth: 0}
      - uses: actions/setup-node@v4
        with: {node-version: 22}
      - run: node loterias-ai/scripts/summarize-meta-pleno-v12.mjs
      - name: Persist safely
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add loterias-ai/data/research/meta-pleno-v12-summary.json
          git diff --cached --quiet && exit 0
          git commit -m 'data(loterias-ai): persist MetaPleno v12 summary'
          for i in 1 2 3 4 5; do git pull --rebase origin main && git push origin HEAD:main && exit 0; sleep $((i*3)); done
          exit 1
