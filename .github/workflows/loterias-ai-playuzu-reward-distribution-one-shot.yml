name: Loterias AI PlayUZU Reward Field One Shot
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'loterias-ai/casino/playuzu/build-reward-field-distribution-v1.mjs'
      - '.github/workflows/loterias-ai-playuzu-reward-distribution-one-shot.yml'
permissions:
  contents: write
jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with: {fetch-depth: 0}
      - uses: actions/setup-node@v4
        with: {node-version: 24}
      - run: node loterias-ai/casino/playuzu/build-reward-field-distribution-v1.mjs
      - run: |
          set -euo pipefail
          git config user.name github-actions[bot]
          git config user.email 41898282+users.noreply.github.com
          git add loterias-ai/casino/playuzu/evidence/internal-reward-field-distribution-v1.json
          git diff --cached --quiet && exit 0
          git commit -m 'research(playuzu): persist internal reward distribution audit'
          git pull --rebase origin main
          git push origin HEAD:main
