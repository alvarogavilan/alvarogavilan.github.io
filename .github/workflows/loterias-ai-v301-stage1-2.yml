name: Loterias AI v301 Stage1-2

on:
  workflow_dispatch:
  push:
    branches:
      - research/metapleno-v301-wasserstein-dynamics
    paths:
      - 'loterias-ai/research/megalab/v301-stage1-2-wasserstein-dynamics.mjs'
      - 'loterias-ai/research/megalab/v301-wasserstein-dynamics-spec.json'
      - '.github/workflows/loterias-ai-v301-stage1-2.yml'

permissions:
  contents: write

concurrency:
  group: loterias-ai-v301-stage1-2
  cancel-in-progress: false

jobs:
  screen:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Run Wasserstein point-process screen
        run: node loterias-ai/research/megalab/v301-stage1-2-wasserstein-dynamics.mjs
      - name: Enforce blind gates
        run: |
          node -e "const x=require('./loterias-ai/data/research/metapleno-v301-stage1-2-selection.json'); if(x.validationTouched||x.postFreezeTouched||x.retuningPerformed||x.realMoneyPass||x.realStakeEUR!==0) process.exit(1)"
      - name: Persist evidence
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add loterias-ai/data/research/metapleno-v301-stage1-2-selection.json
          git commit -m "research(loterias-ai): persist v301 stage1-2 evidence" || exit 0
          git pull --rebase origin "$GITHUB_REF_NAME"
          git push
