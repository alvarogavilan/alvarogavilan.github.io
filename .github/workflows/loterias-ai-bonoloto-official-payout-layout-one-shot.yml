name: Loterias AI Bonoloto Official Payout Layout One Shot
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'loterias-ai/scripts/probe-bonoloto-official-payout-layout.mjs'
      - '.github/workflows/loterias-ai-bonoloto-official-payout-layout-one-shot.yml'
permissions:
  contents: write
jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with: {fetch-depth: 0}
      - uses: actions/setup-node@v4
        with: {node-version: 24}
      - run: node loterias-ai/scripts/probe-bonoloto-official-payout-layout.mjs
      - run: |
          set -euo pipefail
          git config user.name github-actions[bot]
          git config user.email 41898282+users.noreply.github.com
          git add loterias-ai/data/archive/_meta/bonoloto-official-payout-layout-probe.json
          git diff --cached --quiet && exit 0
          git commit -m 'research(bonoloto): persist official payout layout probe'
          git pull --rebase origin main
          git push origin HEAD:main
