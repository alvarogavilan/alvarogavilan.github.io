name: Loterias AI Primitiva Multi-Baseline
on:
  push:
    paths:
      - 'loterias-ai/scripts/primitiva-multibaseline.mjs'
      - '.github/workflows/loterias-ai-primitiva-multibaseline.yml'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  stress:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node loterias-ai/scripts/primitiva-multibaseline.mjs
      - name: Persist result safely
        run: |
          cp loterias-ai/data/backtests/primitiva-multibaseline.json /tmp/primitiva-multibaseline.json
          git fetch origin main
          git reset --hard origin/main
          cp /tmp/primitiva-multibaseline.json loterias-ai/data/backtests/primitiva-multibaseline.json
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add loterias-ai/data/backtests/primitiva-multibaseline.json
          git commit -m 'data(loterias-ai): refresh Primitiva 100-baseline stress test' || exit 0
          git push origin HEAD:main