name: Loterias AI Botemania Unmapped Live ID Resolver V1

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'loterias-ai/casino/jackpots/botemania-unmapped-live-id-resolver-v1.mjs'
      - '.github/workflows/loterias-ai-botemania-unmapped-live-id-resolver-v1.yml'
  push:
    branches: [main]
    paths:
      - 'loterias-ai/casino/jackpots/botemania-unmapped-live-id-resolver-v1.mjs'
      - '.github/workflows/loterias-ai-botemania-unmapped-live-id-resolver-v1.yml'
  schedule:
    - cron: '41 */6 * * *'

permissions:
  contents: write

concurrency:
  group: loterias-ai-botemania-unmapped-live-id-resolver-v1-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  resolve:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Syntax check
        run: node --check loterias-ai/casino/jackpots/botemania-unmapped-live-id-resolver-v1.mjs
      - name: Resolve unmapped live feed IDs against the Botemania sitemap
        run: node loterias-ai/casino/jackpots/botemania-unmapped-live-id-resolver-v1.mjs
      - name: Persist evidence on main only
        if: github.event_name != 'pull_request'
        shell: bash
        run: |
          set -euo pipefail
          EVIDENCE=loterias-ai/casino/jackpots/evidence/botemania-unmapped-live-id-resolver-v1.json
          mkdir -p /tmp/unmapped-v1
          cp "$EVIDENCE" /tmp/unmapped-v1/evidence.json
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git fetch origin main
          git reset --hard origin/main
          mkdir -p "$(dirname "$EVIDENCE")"
          cp /tmp/unmapped-v1/evidence.json "$EVIDENCE"
          git add "$EVIDENCE"
          if git diff --cached --quiet; then exit 0; fi
          git commit -m 'data(casino): refresh unmapped live ID resolver'
          for attempt in 1 2 3 4; do
            if git pull --rebase origin main && git push origin HEAD:main; then exit 0; fi
            sleep $((attempt*2))
          done
          exit 1
