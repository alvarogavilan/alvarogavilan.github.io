name: Loterias AI MetaPleno v18 Bonoloto Consensus Ensemble
on:
  push:
    paths:
      - 'loterias-ai/scripts/meta-pleno-v18-bonoloto-consensus-ensemble.mjs'
      - '.github/workflows/loterias-ai-meta-pleno-v18-bonoloto-consensus-ensemble.yml'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  evaluate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node loterias-ai/scripts/meta-pleno-v18-bonoloto-consensus-ensemble.mjs
      - name: Persist safely
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add loterias-ai/data/research/meta-pleno-v18-bonoloto-consensus-ensemble.json
          git commit -m 'data(loterias-ai): persist Bonoloto v18 consensus ensemble' || exit 0
          git pull --rebase origin main
          git push origin HEAD:main
