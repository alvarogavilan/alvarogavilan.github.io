name: Loterias AI Primitiva Year Prizes
on:
  push:
    paths:
      - 'loterias-ai/scripts/probe-primitiva-year-prizes.mjs'
      - '.github/workflows/loterias-ai-primitiva-year-prizes.yml'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node loterias-ai/scripts/probe-primitiva-year-prizes.mjs
      - name: Persist safely
        run: |
          cp loterias-ai/data/probes/primitiva-year-prizes.json /tmp/primitiva-year-prizes.json
          git fetch origin main
          git reset --hard origin/main
          cp /tmp/primitiva-year-prizes.json loterias-ai/data/probes/primitiva-year-prizes.json
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add loterias-ai/data/probes/primitiva-year-prizes.json
          git commit -m 'data(loterias-ai): refresh annual Primitiva prize probe' || exit 0
          git push origin HEAD:main