name: Loterias AI MetaPleno v133 Primitiva Correlated Null
on:
  workflow_dispatch:
  push:
    branches: [research/metapleno-v133-primitiva-correlated-null]
    paths:
      - 'loterias-ai/scripts/meta-pleno-v133-primitiva-correlated-null.mjs'
      - '.github/workflows/loterias-ai-meta-pleno-v133-primitiva-correlated-null.yml'
jobs:
  research:
    runs-on: ubuntu-latest
    permissions: {contents: write}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: {node-version: 20}
      - run: node loterias-ai/scripts/meta-pleno-v133-primitiva-correlated-null.mjs
      - run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add loterias-ai/data/research/meta-pleno-v133-primitiva-correlated-null.json
          git diff --cached --quiet || git commit -m 'data(loterias-ai): persist v133 Primitiva correlated null'
          git push
