name: Loterias AI MetaPleno v238 Temporal Low-Rank Latent
on:
  workflow_dispatch:
  push:
    branches: [research/metapleno-v238-temporal-lowrank-latent]
    paths:
      - 'loterias-ai/research/metapleno-v238-temporal-lowrank-latent.mjs'
      - '.github/workflows/loterias-ai-metapleno-v238-temporal-lowrank-latent.yml'
permissions:
  contents: write
jobs:
  research:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: {fetch-depth: 0}
      - uses: actions/setup-node@v4
        with: {node-version: '22'}
      - name: Run v238
        run: node loterias-ai/research/metapleno-v238-temporal-lowrank-latent.mjs
      - name: Validate fail-closed
        run: node -e "const r=require('./loterias-ai/data/research/metapleno-v238-temporal-lowrank-latent.json'); if(r.realMoneyPass!==false||r.realStakeEUR!==0||r.candidatePool.theoreticalCostEUR>15) process.exit(1)"
      - name: Persist result
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add loterias-ai/data/research/metapleno-v238-temporal-lowrank-latent.json
          git diff --cached --quiet || git commit -m 'research(loterias-ai): persist v238 temporal low-rank latent factor'
          git push