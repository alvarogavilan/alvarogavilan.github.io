name: Loterias AI MetaPleno v58 Bonoloto Budget8 Ensemble
on:
  workflow_dispatch:
  push:
    paths:
      - 'loterias-ai/scripts/meta-pleno-v58-bonoloto-budget8-ensemble.mjs'
      - '.github/workflows/loterias-ai-meta-pleno-v58-bonoloto-budget8-ensemble.yml'
permissions:
  contents: write
jobs:
  research:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with: {fetch-depth: 0}
      - uses: actions/setup-node@v4
        with: {node-version: 22}
      - run: node loterias-ai/scripts/meta-pleno-v58-bonoloto-budget8-ensemble.mjs
      - name: Persist result safely
        run: |
          cp loterias-ai/data/research/meta-pleno-v58-bonoloto-budget8-ensemble.json /tmp/v58.json
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          for i in 1 2 3 4 5; do
            git fetch origin main
            git reset --hard origin/main
            cp /tmp/v58.json loterias-ai/data/research/meta-pleno-v58-bonoloto-budget8-ensemble.json
            git add loterias-ai/data/research/meta-pleno-v58-bonoloto-budget8-ensemble.json
            git diff --cached --quiet && exit 0
            git commit -m 'data(loterias-ai): persist MetaPleno v58 Budget8 ensemble result'
            git push origin HEAD:main && exit 0
            sleep $((i*3))
          done
          exit 1
