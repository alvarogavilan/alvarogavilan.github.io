name: Loterias AI PlayUZU Blackjack Nuxt One Shot
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'loterias-ai/casino/playuzu/decode-blackjack-nuxt-v1.mjs'
      - '.github/workflows/loterias-ai-playuzu-blackjack-nuxt-one-shot.yml'
permissions:
  contents: write
jobs:
  decode:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with: {fetch-depth: 0}
      - uses: actions/setup-node@v4
        with: {node-version: 24}
      - run: node loterias-ai/casino/playuzu/decode-blackjack-nuxt-v1.mjs
      - run: |
          set -euo pipefail
          git config user.name github-actions[bot]
          git config user.email 41898282+users.noreply.github.com
          git add loterias-ai/casino/playuzu/evidence/blackjack-nuxt-decoded-v1.json
          git diff --cached --quiet && exit 0
          git commit -m 'data(playuzu): persist decoded blackjack Nuxt game objects'
          git pull --rebase origin main
          git push origin HEAD:main
