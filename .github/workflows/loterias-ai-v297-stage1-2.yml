name: Loterias AI v297 Stage1-2 Reservoir Screen
on:
  workflow_dispatch:
  push:
    branches:
      - research/metapleno-v297-reservoir-computing
    paths:
      - '.github/workflows/loterias-ai-v297-stage1-2.yml'
permissions:
  contents: write
concurrency:
  group: loterias-ai-v297-stage1-2-${{ github.ref }}
  cancel-in-progress: false
jobs:
  research:
    runs-on: ubuntu-24.04
    timeout-minutes: 6
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Run v297 reservoir screening
        run: node loterias-ai/research/megalab/v297-stage1-2-reservoir-screen.mjs
      - name: Verify isolation and money gates
        run: node -e "const x=require('./loterias-ai/data/research/metapleno-v297-stage1-2-selection.json');if(x.validationTouched||x.postFreezeTouched||x.retuningPerformed||x.realMoneyPass||x.realStakeEUR!==0||x.ordinaryBudgetMaxEUR>4)process.exit(1)"
      - name: Persist v297 selection evidence
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add loterias-ai/data/research/metapleno-v297-stage1-2-selection.json
          git commit -m 'data(loterias-ai): persist v297 reservoir stage1-2' || exit 0
          git pull --rebase origin research/metapleno-v297-reservoir-computing
          git push
