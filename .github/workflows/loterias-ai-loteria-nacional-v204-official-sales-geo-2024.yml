name: Loterias AI Loteria Nacional v204 Official Sales Geo 2024
on:
  workflow_dispatch:
  push:
    branches: [research/loteria-nacional-v204-official-sales-geo-2024]
permissions:
  contents: write
jobs:
  research:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: {node-version: '22'}
      - run: node loterias-ai/scripts/loteria-nacional-v204-official-sales-geo-2024.mjs
      - name: Validate official-only geography
        run: |
          node - <<'NODE'
          const fs=require('fs');const p='loterias-ai/data/research/loteria-nacional-v204-official-sales-geo-2024.json';const d=JSON.parse(fs.readFileSync(p));
          if(d.provider!=='SELAE'||d.scope?.year!==2024) throw new Error('Unexpected provider/year');
          if(d.realMoneyPass!==false) throw new Error('Real-money gate regression');
          console.log(JSON.stringify(d.summary));
          NODE
      - name: Persist v204
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add loterias-ai/data/research/loteria-nacional-v204-official-sales-geo-2024.json
          if ! git diff --cached --quiet; then git commit -m 'data(loterias-ai): persist v204 Loteria Nacional geo 2024'; git pull --rebase origin "$GITHUB_REF_NAME"; git push origin HEAD:"$GITHUB_REF_NAME"; fi
