name: Loterias AI Million Formula Search
on:
  workflow_dispatch:
  push:
    paths:
      - 'loterias-ai/scripts/mass-formula-strategy-shard.mjs'
      - 'loterias-ai/scripts/aggregate-mass-formula-strategies.mjs'
      - '.github/workflows/loterias-ai-million-formula-search.yml'
permissions:
  contents: write
concurrency:
  group: loterias-ai-million-formula-search
  cancel-in-progress: false
jobs:
  search:
    strategy:
      fail-fast: false
      matrix:
        game: [primitiva, bonoloto, euromillones, eurodreams, gordo-primitiva]
        shard: [0, 1, 2, 3]
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Evaluate 50,000 deterministic formulas
        env:
          GAME: ${{ matrix.game }}
          SHARD: ${{ matrix.shard }}
          CANDIDATES: 50000
        run: node loterias-ai/scripts/mass-formula-strategy-shard.mjs
      - uses: actions/upload-artifact@v4
        with:
          name: formulas-${{ matrix.game }}-${{ matrix.shard }}
          path: /tmp/loterias-ai-formulas/${{ matrix.game }}-${{ matrix.shard }}.json
          retention-days: 2
  aggregate:
    needs: search
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - uses: actions/download-artifact@v4
        with:
          pattern: formulas-*
          path: /tmp/formulas
          merge-multiple: true
      - name: Freeze formula champions and open final holdout
        run: node loterias-ai/scripts/aggregate-mass-formula-strategies.mjs
      - name: Persist evidence safely
        run: |
          cp loterias-ai/data/research/mass-formula-strategy-search.json /tmp/mass-formula-strategy-search.json
          git fetch origin main
          git reset --hard origin/main
          mkdir -p loterias-ai/data/research
          cp /tmp/mass-formula-strategy-search.json loterias-ai/data/research/mass-formula-strategy-search.json
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add loterias-ai/data/research/mass-formula-strategy-search.json
          git commit -m 'data(loterias-ai): persist one-million deterministic formula search' || exit 0
          git push origin HEAD:main
