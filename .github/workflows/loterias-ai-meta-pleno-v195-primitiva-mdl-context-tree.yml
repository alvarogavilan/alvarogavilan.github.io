name: Loterias AI MetaPleno v195 Primitiva MDL Context Tree
on:
  workflow_dispatch:
  push:
    branches: [research/metapleno-v195-primitiva-mdl-context-tree]
permissions:
  contents: write
jobs:
  research:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: node loterias-ai/scripts/meta-pleno-v195-primitiva-mdl-context-tree.mjs
      - name: Persist v195
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add loterias-ai/data/research/meta-pleno-v195-primitiva-mdl-context-tree.json
          if ! git diff --cached --quiet; then
            git commit -m "research(loterias-ai): persist v195 Primitiva MDL"
            git pull --rebase origin "$GITHUB_REF_NAME"
            git push origin HEAD:"$GITHUB_REF_NAME"
          fi
