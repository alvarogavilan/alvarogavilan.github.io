name: Loterias AI Quintuple Plus Official Financial Tournament
on:
  push:
    paths:
      - 'loterias-ai/scripts/quintuple-plus-official-financial-tournament.mjs'
      - 'loterias-ai/data/archive/quintuple-plus/**'
      - '.github/workflows/loterias-ai-quintuple-plus-official-financial-tournament.yml'
  workflow_dispatch:
permissions:
  contents: write
concurrency:
  group: loterias-ai-quintuple-plus-financial-tournament
  cancel-in-progress: true
jobs:
  tournament:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: {node-version: 22}
      - run: node loterias-ai/scripts/quintuple-plus-official-financial-tournament.mjs
      - name: Persist safely
        run: |
          cp loterias-ai/data/backtests/quintuple-plus-official-financial-tournament.json /tmp/quintuple-backtest.json
          git fetch origin main
          git reset --hard origin/main
          mkdir -p loterias-ai/data/backtests
          cp /tmp/quintuple-backtest.json loterias-ai/data/backtests/quintuple-plus-official-financial-tournament.json
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add loterias-ai/data/backtests/quintuple-plus-official-financial-tournament.json
          git commit -m 'data(loterias-ai): persist Quíntuple Plus official financial tournament' || exit 0
          git push origin HEAD:main
