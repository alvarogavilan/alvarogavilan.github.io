name: Loterias AI Gordo Soft Triple Economic Paired
on:
  workflow_dispatch:
  push:
    paths:
      - 'loterias-ai/scripts/gordo-soft-triple-economic-paired.mjs'
      - '.github/workflows/loterias-ai-gordo-soft-triple-economic-paired.yml'
permissions:
  contents: write
jobs:
  settle:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with: {fetch-depth: 0}
      - uses: actions/setup-node@v4
        with: {node-version: 20}
      - name: Liquidate against complete official SELAE economics
        run: node loterias-ai/scripts/gordo-soft-triple-economic-paired.mjs
      - name: Persist official economic evidence
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add loterias-ai/data/research/gordo-soft-triple-economic-paired.json
          git diff --cached --quiet && exit 0
          git commit -m 'data(loterias-ai): persist complete official Gordo soft-triple economics'
          for i in 1 2 3 4 5; do
            git fetch origin main
            git rebase origin/main && git push origin HEAD:main && exit 0
            git rebase --abort || true
            sleep $((i*3))
          done
          exit 1
