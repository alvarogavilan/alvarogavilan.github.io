name: Loterias AI Bonoloto Conflict Audit
on:
  workflow_dispatch:
  push:
    paths:
      - 'loterias-ai/scripts/audit-bonoloto-official-conflict.mjs'
      - '.github/workflows/loterias-ai-bonoloto-conflict-audit.yml'
permissions:
  contents: write
jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with: {fetch-depth: 0}
      - uses: actions/setup-node@v4
        with: {node-version: 22}
      - run: node loterias-ai/scripts/audit-bonoloto-official-conflict.mjs
      - name: Persist audit safely
        run: |
          cp loterias-ai/data/audits/bonoloto-2022-06-24-conflict.json /tmp/bono-conflict.json
          git fetch origin main
          git reset --hard origin/main
          mkdir -p loterias-ai/data/audits
          cp /tmp/bono-conflict.json loterias-ai/data/audits/bonoloto-2022-06-24-conflict.json
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add loterias-ai/data/audits/bonoloto-2022-06-24-conflict.json
          git commit -m 'data(loterias-ai): persist Bonoloto conflict audit' || exit 0
          git push origin HEAD:main
