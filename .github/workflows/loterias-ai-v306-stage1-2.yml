name: Loterias AI v306 Stage1-2

on:
  workflow_dispatch:
  push:
    branches:
      - research/metapleno-v306-conformal-residual-ranks
    paths:
      - 'loterias-ai/research/megalab/v306-stage1-2-conformal-residual-ranks.mjs'
      - 'loterias-ai/research/megalab/v306-conformal-residual-rank-spec.json'
      - '.github/workflows/loterias-ai-v306-stage1-2.yml'

permissions:
  contents: write

concurrency:
  group: loterias-ai-v306-stage1-2
  cancel-in-progress: false

jobs:
  screen:
    runs-on: ubuntu-latest
    timeout-minutes: 6
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Run causal conformal residual rank screen
        run: node loterias-ai/research/megalab/v306-stage1-2-conformal-residual-ranks.mjs
      - name: Enforce blind gates
        run: |
          node -e "const x=require('./loterias-ai/data/research/metapleno-v306-stage1-2-selection.json'); if(x.validationTouched||x.postFreezeTouched||x.retuningPerformed||x.realMoneyPass||x.realStakeEUR!==0||x.ordinaryBudgetMaxEUR>4) process.exit(1)"
      - name: Persist evidence
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add loterias-ai/data/research/metapleno-v306-stage1-2-selection.json
          git commit -m "research(loterias-ai): persist v306 stage1-2 evidence" || exit 0
          git pull --rebase origin "$GITHUB_REF_NAME"
          git push
