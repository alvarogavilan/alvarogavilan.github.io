name: Loterias AI MetaPleno v57 Bonoloto Hazard Budget8
on:
  workflow_dispatch:
  push:
    paths:
      - 'loterias-ai/scripts/meta-pleno-v57-bonoloto-conditional-hazard-budget8.mjs'
      - '.github/workflows/loterias-ai-meta-pleno-v57-bonoloto-hazard-budget8.yml'
permissions:
  contents: write
concurrency:
  group: loterias-ai-meta-pleno-v57-bonoloto-hazard-budget8
  cancel-in-progress: false
jobs:
  shards:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    strategy:
      fail-fast: false
      matrix:
        shard: [0,1,2,3,4,5,6,7]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: {node-version: 22}
      - name: Search conditional-hazard Budget8 models
        env:
          SHARD_ID: ${{ matrix.shard }}
          PER_SHARD: 25000
        run: node loterias-ai/scripts/meta-pleno-v57-bonoloto-conditional-hazard-budget8.mjs
      - uses: actions/upload-artifact@v4
        with:
          name: v57-shard-${{ matrix.shard }}
          path: v57-out/*.json
          retention-days: 2
  aggregate:
    needs: shards
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with: {fetch-depth: 0}
      - uses: actions/setup-node@v4
        with: {node-version: 22}
      - uses: actions/download-artifact@v4
        with:
          pattern: v57-shard-*
          path: v57-shards-raw
      - name: Flatten shards
        run: |
          mkdir -p v57-shards
          find v57-shards-raw -name '*.json' -exec cp {} v57-shards/ \;
      - name: Aggregate and open later eras
        env:
          AGGREGATE: 1
          SHARD_DIR: v57-shards
        run: node loterias-ai/scripts/meta-pleno-v57-bonoloto-conditional-hazard-budget8.mjs
      - name: Persist result safely
        run: |
          cp loterias-ai/data/research/meta-pleno-v57-bonoloto-conditional-hazard-budget8.json /tmp/v57.json
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          for i in 1 2 3 4 5; do
            git fetch origin main
            git reset --hard origin/main
            cp /tmp/v57.json loterias-ai/data/research/meta-pleno-v57-bonoloto-conditional-hazard-budget8.json
            git add loterias-ai/data/research/meta-pleno-v57-bonoloto-conditional-hazard-budget8.json
            git diff --cached --quiet && exit 0
            git commit -m 'data(loterias-ai): persist MetaPleno v57 hazard Budget8 result'
            git push origin HEAD:main && exit 0
            sleep $((i*3))
          done
          exit 1
